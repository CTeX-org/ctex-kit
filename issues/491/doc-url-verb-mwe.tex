\documentclass{article}
\usepackage[a4paper,margin=18mm]{geometry}
\usepackage{xcolor,xeCJK,url,doc}
\setCJKmainfont[ItalicFont=FandolFang-Regular.otf]{FandolSong-Regular.otf}
\pagestyle{empty}
\setlength\parindent{0pt}
\newcommand\pkgversion{\csname ver@xeCJK.sty\endcsname}
\newcommand\pair[3]{%
  \par\medskip\textbf{#1}\par
  \setbox0=\hbox{#2}\setbox2=\hbox{#3}%
  \fbox{\copy0}\quad reference (\the\wd0)\par\smallskip
  \fbox{\copy2}\quad command (\the\wd2)\par
  \dimen0=\wd2 \advance\dimen0 by -\wd0
  \ifdim\dimen0=0pt\textcolor{teal}{same width}\else
    \textcolor{red}{difference: \the\dimen0}\fi\par}
\begin{document}
{\Large\bfseries Documentation commands, URL, and verbatim}\par
xeCJK: \texttt{\pkgversion}

\makeatletter
\pair{Documentation target hbox (\#873)}
  {用 abc 设置}
  {用 \HD@target abc 设置}
\makeatother

\pair{Compact CJK content in \string\meta (\#920/\#951)}
  {$\langle$\hbox{参数}$\rangle$}
  {\meta{参数}}

\medskip\textbf{URL boundary (\#880)}\par
\setbox0=\hbox{测\hbox{}\url{X}无}%
\setbox2=\hbox{测\url{X}无}%
\fbox{\copy0}\quad no-left-glue control (\the\wd0)\par\smallskip
\fbox{\copy2}\quad actual URL boundary (\the\wd2)\par
\dimen0=\wd2 \advance\dimen0 by -\wd0
\ifdim\dimen0>2pt
  \textcolor{teal}{left ecglue present: +\the\dimen0}%
\else
  \textcolor{red}{left ecglue missing: \the\dimen0}%
\fi\par

\medskip\textbf{\string\verb boundary (\#910/\#919)}\par
\setbox0=\hbox{用 \verb|command| 无}%
\setbox2=\hbox{用无}%
\setbox4=\hbox{\verb|command|}%
\fbox{\copy0}\quad complete width \the\wd0\par
\dimen0=\wd0 \advance\dimen0 by -\wd2 \advance\dimen0 by -\wd4
boundary spacing: \the\dimen0\par

\medskip
The frames make missing or doubled CJK--Latin spacing visible at both ends.
\end{document}
