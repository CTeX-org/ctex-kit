\documentclass{article}
\usepackage[a4paper,margin=18mm]{geometry}
\usepackage{xcolor,xeCJK,hyperref}
\setCJKmainfont{FandolSong-Regular.otf}
\pagestyle{empty}
\setlength\parindent{0pt}
\newcommand\pkgversion{\csname ver@xeCJK.sty\endcsname}
\newcommand\pair[3]{%
  \par\medskip\textbf{#1}\par
  \setbox0=\hbox{#2}\setbox2=\hbox{#3}%
  \fbox{\copy0}\quad reference (\the\wd0)\par\smallskip
  \fbox{\copy2}\quad command (\the\wd2)\par
  \dimen0=\wd2 \advance\dimen0 by -\wd0
  \ifdim\dimen0=0pt\textcolor{teal}{same width}\else
    \textcolor{red}{difference: \the\dimen0}\fi\par}
\newcommand\cjkmacro{方程}
\begin{document}
{\Large\bfseries Macro, box, and hyperlink boundaries}\par
xeCJK: \texttt{\pkgversion}

\pair{Source newline before a macro (\#324)}
  {参见\cjkmacro}
  {参见
   \cjkmacro}

\pair{Explicit group and source space (\#831)}
  {前中 后}
  {前{中} 后}

\pair{Box boundary and source spaces (\#831)}
  {前 中 后}
  {前 \mbox{中} 后}

\pair{Hyperlink annotation around a digit (\#809)}
  {图1图}
  {图\hyperref[target]{1}图}

\pair{No spurious glue inside a CJK link (\#810)}
  {信箱和冰箱会领导}
  {\hyperref[target]{信箱和冰箱会领导}}
\hypertarget{target}{}
\end{document}
