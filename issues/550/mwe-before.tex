% MWE: #550 之前 —— 没有查询命令, 只能手工给索引条目加拼音前缀
\documentclass{article}
\usepackage{xeCJK}
\setCJKmainfont{FandolSong-Regular.otf}
\usepackage{xpinyin}
\makeindex
\begin{document}
\section*{问题}

xpinyin 只能把拼音\emph{排}在汉字上方:

\xpinyin*{汉字}\quad\xpinyin*{安全}\quad\xpinyin*{中国}

但拿不到「这个字读什么」这段文字。做中文索引时, CLDR 默认把中英文混排,
想按拼音首字母分组, 只能手工给每个条目写前缀:

\index{han@汉字}汉字\quad
\index{an@安全}安全\quad
\index{zhong@中国}中国

条目一多就难以维护, 且容易写错。
\end{document}
