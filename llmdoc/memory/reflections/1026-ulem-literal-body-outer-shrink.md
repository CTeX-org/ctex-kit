---
name: 1026-ulem-literal-body-outer-shrink
description: 记录 #1026 修复 xeCJKfntef 装饰右边界断行不对齐时，正文从字面记号改成宏参数导致收缩量被固化在片段盒内部的根因，以及三次回归测试写错都“通过”的教训
metadata:
  type: feedback
---

# [Task Reflection]

## Task

Issue #1026：`xeCJK-v3.10.5-rc1` 之后，`\CJKunderline`／`\CJKunderwave`／`\CJKunderanyline*` 等装饰在换行时右边界不对齐。用户以为是 #1012 的周期装饰改动引入，实际不是；引入点在更早的 `494d5a72`（#1002 的一部分）。

修复方式：`xeCJK/xeCJK.dtx` 的 `\UL@on` 与 `\UL@onin` 改为先判断语法，再分别走两条路径——正文是“公式尾＋尾随源码空格”时用 `\@@_boundary_ulem_math_tail_space:nnn` 重排（保留 #1002 的修复），否则保持 `\xeCJK_ulem_left: #1` 的字面展开。新增 `\@@_boundary_if_ulem_math_reorder:nTF` 判断走哪条路径，原来统一处理两种情况的 `\@@_boundary_ulem_math_body:n` 被这两个函数取代。新增回归 `xeCJK/testfiles/fntef-shrink01.lvt/.tlg`。

重要补充：第一版修复只改好了非重排路径。重排分支仍把正文交给辅助宏的参数，因此在这条路径上原封不动保留了同一个缺陷——正文只要既含西文词、又以“公式＋尾随空格”结尾，溢出量与修复前完全相同。这是独立审查发现的，我自己的验证没覆盖到，原因见下面第 6 条。最终改为先拼装进 `\l_@@_ulem_body_tl`、再用 `\exp_args:NV` 一次展开到参数位置。

## Expected vs Actual

- 预期：既然标签和现象都指向 fntef 装饰，根因应该在 #1012 的周期 leaders 或裁切逻辑里，按接点分工那份决策文档定位即可。
- 实际：`git bisect` 式排查证明 #1012 的四个提交都不是引入点；真正的引入点是更早的 `494d5a72`（#1002 inline-math 系列），把 `\UL@on` 的替换文本从 `\xeCJK_ulem_left: #1 \@@_boundary_math_end:n {#1}`（字面记号）改成了 `\xeCJK_ulem_left: \@@_boundary_ulem_math_body:n {#1}`（宏参数间接展开）。子系统标签和现象所指的位置，可以与真正的引入提交完全无关。
- 预期：把内容装进 `\hbox` 量 badness，或者用 `\vbox` 排段落，就能测出收缩量差异。
- 实际：连续写了三版测试都“通过”，但都是假绿——`\hbox`／`\vbox` 的 glue set 会把内外层收缩一并用掉，两版数字完全相同；第三版用 `\def\BODY` 承载正文，宏体本身就触发既有限制，两版又相同。只有“document 主垂直列表里成段排版 + 调用处字面正文”才能分辨出差异。

## What Went Wrong

1. **第一假设方向错了。** Issue 标题和标签都指向 fntef 装饰，先读了 #1012 的决策文档和周期 leaders 实现，排查了半天才确认这条线索是错的，浪费了定位时间。

2. **测试写错三次，每次都显示通过。**
   - 第一版：把正文装进单个 `\hbox` 量 badness——`\hbox` 内部会把所有可用收缩量统一用掉抹平差异，缺陷版与修复版数字完全相同。
   - 第二版：改用 `\vbox` 排段落——同样量不出，两版都是 18.08pt。
   - 第三版：在 `\TEST` 里用 `\def\BODY` 承载正文——宏体本身就会触发既有限制（见下），两版又相同。
   直到第四版改成“document 主垂直列表里成段排版，且调用处写字面正文”才第一次看到差异。

3. **一开始没有先用重新引入缺陷的方式验证测试会失败。** 前三版测试全部显示通过，如果没有主动怀疑并倒回去验证，很可能把假绿当成“修复已验证”上报。

4. **`\showthe` 在 `\TEST` 里会中断执行。** 排查过程中用 `\showthe` 打印中间宽度，导致 `.tlg` 只记录到第一个 `\TEST` 就停止；目标宽度不可达时 `\hbox to` 还会把整份节点列表倒进 `.tlg` 当基线，非常脆弱，容易把调试残留误固化成基线。

5. **并行跑 `l3build save` 和 `l3build check` 互相清掉了共享的 `build/test` 目录**，出现 `No such file or directory` 的假失败，一度误判为测试脚本本身有问题。

6. **对比实验里又踩了一次“实际加载路径与预期不符”的坑。** `TEXINPUTS` 指向工作树后，仍需要在日志里核对实际加载的是不是工作树版本；有几轮对比误以为在测工作树，实际命中的是系统 TeX Live 的同名文件，得出“两者相同”的错误结论。这与 #1012 反思里记录的教训同源，但这次是在对比实验环节而非验证环节踩的。

7. **只验证了自己想到的那条路径，漏掉了保留下来的另一条。** 定位到根因是“正文经宏参数间接展开”之后，我把非重排路径改回字面展开就收工了，没有回头问“被保留的重排分支是不是也走同一条间接展开”。实际它正是——同一根因在同一函数里还剩一份，只是触发条件更窄（正文需同时含西文词并以公式加空格结尾）。教训：确认根因后，应当把该根因当作判据去枚举**所有**满足它的代码路径，而不是只修触发当前复现样例的那一条。这是本轮独立审查发现的第一个重要问题。

8. **给一条行为指定“守护测试”时按名义职责推断，没有变异实测。** 我在测试注释和 llmdoc 里写 TEST 5 负责固定“重排确实发生、尾随空格仍被装饰”，但它的 `.tlg` 其实一行输出都没有：内容短、不触发 overfull，而该用例只以 overfull 报告为观察通道。审查用变异证明：关掉重排、甚至删掉交还 `ulem` 的空格，它都照样通过；真正拦住关掉重排的是 `command-boundary-math05`。教训：声称某测试守护某行为之前，必须用变异实测确认它真的会红；空基线不构成任何门禁，而写在文档里的错误归属会让后来者误以为已有覆盖。

9. **用 `\showbox` 写测试，静默吞掉了其后所有用例。** 为了给空基线补上观察通道，我改用 `\showbox` 打印装饰盒。但 `checkopts` 带 `-halt-on-error`，`\showbox` 抛出的 `! OK.` 让编译当场终止：之后新增的用例一次都没跑过，而 `l3build check` 始终报绿。是审查在 `\END` 前插探针 `\TEST` 并确认它没进日志才发现的。仓库里 `verb-ecglue02.lvt` 早把这条坑写成注释并给出 `\loggingoutput` + `\box` 的既定做法——我没有先去看同类测试怎么做，直接用了最顺手的原语。

10. **断言强度低于所声称的行为，自己却没有反向构造反例。** 修好空基线后，我只固定装饰盒总宽度，注释和文档仍写“尾随空格仍被装饰”。审查用三种“宽度不变但空格确实没被装饰”的变异（等宽 kern、`\UL@stop` 包裹、改走外层 glue 通道）证明它全部照常通过。教训与第 8 条同源但更深一层：不只要问“这个测试会不会红”，还要问“有没有一种实现能让它绿而我声称的行为已经错了”。

11. **同一轮里连续写错了三处事实描述。** “片段数会变小”“末尾整段装饰片段消失”都经探针证伪（片段数前后都是 5 段，只是末段从 11.04524pt 缩到 7.71524pt）；把守护职责归给 TEST 5 也是错的（真正会红的是 `command-boundary-math05`）。共同成因是凭对机制的推断写描述，而没有把描述本身当作待验证的断言去实测。

12. **换观察通道时没有查它的副作用，也没有复查体积。** 改用 `\loggingoutput` 后我把它写在 `\showboxbreadth=1 \showboxdepth=0` 之后，而它的定义会把这两个参数覆盖成 `\maxdimen`——前四项因此也倒出完整节点列表，`.tlg` 从预期的百余行涨到 3279 行、含 880 处绘图 `special`，恰好违反同一文件和 `build-and-test.md` 里“只固定行盒尺寸与 glue set，不比对装饰图形”的设计。修正顺序并给 `\box` 补上 `\clearpage` 后降到 145 行。教训：引入一个会改全局状态的原语时要先读它的定义；生成基线后应当核对体积与内容是否就是想固定的东西，而不是只看 check 是否为绿。

13. **同一条职责归属被我改错了两次。** 先是把守护职责写给一个空基线；改成只量总宽度后又写了一次“本文件不负责重排是否发生”；等节点列表进入基线，这句话再次变成假的——变异实测证明它现在确实会红。教训：每次调整观察通道，都要重新跑一遍全部相关变异，并同步复查所有引用该归属的文字，不能只改代码。

14. **一致性修改的那一半没有门禁，我却把两者一起写进了覆盖声明。** `\UL@onin` 的重排分支按同一约束改了，但 `ulem` 的 `\UL@onin` 用 `\setbox\UL@box\hbox{{#1}}` 把内容整体装进 hbox，内层收缩量出不了这个盒子——症状在嵌套路径上根本不显现。实测在该分支重新引入缺陷、乃至删掉整段重排分支，全套 114 项都全绿，嵌套盒子的宽高深逐位相同。而 `build-and-test.md` 原本写的是“固定 `\UL@on` / `\UL@onin` …这条约束”，读起来像两者都有覆盖。教训：当一处改动是“为了一致性顺手改的”，要单独确认它有没有门禁；没有就如实写明依赖代码审查，不要让它蹭到另一半的覆盖声明里。

15. **同一根因还剩第三处，而且这次修不动。** 枚举代码路径时我只看了“正文怎么进参数”，没想到重排**自己插回去的那枚空格**也落在片段盒内——外层收缩量因此比发布版少 1.11pt。尝试改走已有外层通道后收缩量对上了，却让这枚空格对边界机制变得可见、被计算两次，`command-boundary-math01` 直接报 3.33pt boundary delta 失败。最终按“已接受的限制”记录：可观察影响为零（`\hsize` 180–260pt 扫描下不改变断行），真要修得连同 #1002 的 `math-space` 语义一起重新设计。教训：枚举根因路径时，不只要看输入怎么进来，还要看这段代码自己产出的节点落在哪一层；而修复方案本身也要跑相邻契约的回归，不能只看目标指标是否回正。

## Root Cause

`\UL@on` 的正文原本以字面记号留在替换文本里；#1002 为了处理“公式加尾随源码空格”这一种情况，把正文统一改成了通过宏参数间接展开。ulem 自己扫描正文并按源码空格把它切成固定宽度的装饰片段盒（每个片段一个盒子）；正文一旦经过参数间接展开，西文词右侧由边界恢复链补出的 `\CJKecglue` 就会落在片段盒**内部**，其收缩量被盒子固化，无法参与外层段落的断行决策。这一改动本意只是为了让“公式加尾随源码空格”这一种边界情况能正确重排，却把所有正文都改成了间接展开，波及了不需要重排的普通场景。

节点级证据（`\showbox` 深度）：基线中 `hello` 后的 ecglue 在深度 1（片段之间），是 `\cleaders 5.32997 plus 1.665 minus 1.11`；缺陷版中同一枚 ecglue 落在深度 2（片段盒内部），变成 `\cleaders 1.99997 plus 0.96002`，没有 minus。两者自然宽度完全相同（328.08pt），但深度 1 的可收缩总量从 17.22pt 降到 15.00pt，差 2.22pt，正好等于 2 个西文词 × 每词 1.11pt 的收缩量。原始 MWE 的行右端从基线 681px 冲到 722px。

测试写不出来的根因是三版方案分别选错了“能观察到内外层区分”的载体：单 `\hbox`／`\vbox` 都会把外层收缩一并耗尽，抹掉内外层的区别；宏承载正文则是从另一个通道触发了同一类“收缩量出不了外层”的既有限制，恰好和本次缺陷的症状一致却成因不同，掩盖了两者的差异。

## 已接受的既有限制（重要，不在本次修复范围）

把正文写成宏再传入，例如 `\CJKunderline{\BODY}`，收缩量同样进不了外层。实测**发布版本（系统 TeX Live）同样是 18.08pt**，说明这不是本次回归，而是发布版本本来就有的既有限制。这条限制没有修，测试因此必须在调用处写字面正文，不能用宏承载正文，否则会把这条已知限制误判成本次修复的验证对象。

## Missing Docs or Signals

- `xecjk-architecture.md` 目前只记录了 #1002 的 `math-space`／`math-space-frozen` 区分逻辑，没有说明“ulem 正文必须以字面记号留在替换文本、不能经宏参数间接展开”这条更基础的约束；这条约束比公式候选确认更早生效，属于 ulem 集成层面的通用限制。
- 没有一处文档提醒“用单个 `\hbox`／`\vbox` 测收缩量会被 glue set 抹平内外层差异”；这是继 #1002 反思里“总宽度会掩盖左右一多一少”之后，同一主题（弹性 glue 的三项参数与实际断行）下的第二个具体反例，应该合并进已有的相关经验条目。
- “调用处写宏 vs 写字面正文”的区分目前只留在这份反思和测试文件注释里，没有进入 `coding-conventions.md` 或 fntef 相关的架构文档，后续再有类似 issue 容易重新踩这条既有限制当作新缺陷去修。

## Promotion Candidates

- **正文经宏参数间接展开会让内部装饰盒固化收缩量，无法归纳为“用户没按预期方式调用”。** ulem／fntef 等自行扫描正文并切片装盒的机制，只应对正文使用字面记号；确需重排某种特殊语法（如公式尾＋空格）时，应该用条件判断只对该语法走重排路径，其余情况保持字面展开，不能为了少数情况把全部正文都改成间接展开。
- **量弹性 glue 的收缩量是否真的能参与断行，不能只用单个 `\hbox`／`\vbox` 包起来测；必须放进 document 主垂直列表里真正跑一次断行。** 这是对已有“弹性 glue 的三项参数与实际断行”教训的补充反例，建议在 `lessons-learned.md` 对应条目里追加这一具体反例说明。
- **子系统标签和现象所指的位置，不能代替 bisect 定位真正的引入提交。** 相关 issue 之间即使表面现象相似（同为 fntef 装饰、同为换行相关），引入点也可能落在完全不同的历史提交里；排查顺序应该是先 bisect 确认引入点，再去读对应提交的决策文档，不要反过来。
- **调用处用字面正文 vs 用宏承载正文，是两条不同的收缩链路，测试必须明确区分并分别验证。** 用宏承载正文触发的是已知既有限制，不应该混进本次缺陷的验证范围。
- **确认根因后应把它当判据枚举全部满足该根因的代码路径，而不是只修触发复现样例的那一条。** 本轮第一版修复就漏掉了同一函数里保留下来的另一条间接展开路径。
- **声称某测试守护某行为，必须用变异实测确认它真的会红。** 空基线（没有任何输出行的 `.tlg` 段）不构成门禁；把守护职责写错到文档里，会让后来者误以为已有覆盖。
- **l3build 测试里不能用 `\showbox`（`-halt-on-error` 会让它静默截断其后所有用例）；需要节点列表时用 `\loggingoutput` + `\box` + `\clearpage`。**
- **断言强度要匹配所声称的行为：先反向构造“尺寸不变但行为已错”的实现，若存在就必须把结构纳入基线。**

## Follow-up

- 若后续要修复“正文经宏参数传入导致收缩量丢失”这条既有限制，需要新开 issue；本次修复的范围明确不含它。
- recorder 后续同步 `xecjk-architecture.md` 时，补充 ulem 集成层“正文必须字面记号、不能经参数间接展开”这条约束，并关联本反思与 #1002 的 `math-space` 逻辑，说明两者的分工边界（#1002 只解决“公式尾＋空格”这一种需要重排的情况，其余场景必须保持字面）。
- `lessons-learned.md` 中“弹性 glue 的三项参数与实际断行”条目可以补充“单 `\hbox`／`\vbox` 会被 glue set 抹平内外层区分，须在 document 主垂直列表里断行才能验证”这一具体反例。
- 后续遇到 fntef／ulem 相关 issue 时，先确认标签指向的子系统是否真的是引入点，再决定读哪份决策文档。

## 相关

- Issue：#1026；引入点 `494d5a72`（属于 #1002）；受影响版本 `xeCJK-v3.10.5-rc1`；被误判方向的相关工作 #1012。
- 实现：`xeCJK/xeCJK.dtx` 中的 `\UL@on`、`\UL@onin`、`\@@_boundary_if_ulem_math_reorder:nTF`、`\@@_boundary_ulem_math_tail_space:nnn`。
- 测试：`xeCJK/testfiles/fntef-shrink01.lvt/.tlg`。
- 相关反思与决策：[[1002-inline-math-boundary]]、[[../decisions/1002-inline-math-boundary-oracle.md]]、[[1012-fntef-decoration-overlap.md]]。
