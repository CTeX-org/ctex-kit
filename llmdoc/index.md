# llmdoc 索引

## overview

- `llmdoc/overview/project-overview.md` — 项目范围、仓库组织、核心/卫星包分类、技术栈与维护状态；现含三条本地 Agent workflow、六个 Agent job 以 runner 默认用户运行并拥有完整本地执行权限（#1032 起）、单脚本工具安装、restore-only 缓存，以及只读 `GITHUB_TOKEN`／独立 publisher／Claude `--bare` 的纯权限隔离与已接受风险；「维护状态」现记录 CI 独立回归包已扩展到 `xpinyin/`（#1041 起，覆盖 XeTeX/xeCJK 与 CJKutf8/pdfTeX 两条路线）。2026-08-13 起三条 workflow 的 Codex 主链路统一使用 `continue-on-error` + `outputs.status` 汇总，失败以 warning 触发 Claude fallback，只有两条链路都失败才让最终 job 显红；Codex 默认 endpoint、`priority` 通道和 `high` 推理强度也已固化，详见 `llmdoc/reference/build-and-test.md`。

## architecture

- `llmdoc/architecture/package-architecture.md` — `ctex` 与 `xeCJK` 的主干架构、引擎适配策略、第三方包补丁子系统与包间依赖图；现含 #381 的 LuaTeX 拒绝传统 `CJKfntef` / XeTeX 透明替换边界、#407/#800 的 `\xeCJKchar` + 定点补丁策略、#158 Hangul L/V/T、#165 `CJStarter`，以及 #992 按实际首尾类别工作的 capture/register 高层模型和 PR #1005 对右边界后续恢复状态的补充；「Linestretch 与 CJKglue 子系统」现记录 #1068 后 `\@@_update_stretch_auxii:` 的引擎特化覆写对全部引擎生效（此前被 docstrip 守卫限定在 pdftex/xetex，luatex/uptex 从未执行过该重定义，各自的判断逻辑早已存在只是缺调用点）。
- `llmdoc/architecture/xecjk-architecture.md` — xeCJK 独立架构详解：
  - interchar token、字符分类（#158/#165/#336/#347/#382）、基础 marker/glue 恢复逻辑、#992 capture/register 策略与直接输入比较、`xCJKecglue=false/true` 双值矩阵、嵌套和 `\sbox` 隔离、TeX 无法区分某些显式 glue 与源码空格的限制，以及 `\kern0pt` 处理方法；其中 PR #1001 为 Boundary→CJK 和 Boundary→Default 使用相同的 glue 检查（#996），并在 box/wrapped-box 结束时同时检查盒子尺寸与末节点类型，按 Default 处理 math/rule 等可见内容，再通过嵌套盒子留下的 marker 逐层更新末类别（#998）；PR #1005 为 #1003 恢复外层 `spacefactor` 与 post-transparent 的有界 `marker + glue` 后缀；#1002 还按空格是否参与外层断行区分 `math-space` 与 `math-space-frozen`；#1010 增加实验性用户注册入口，记录导言区末尾应用、`AtBeginDocument` 后存在性检查，以及通用注册表与专用适配器保留表并集这一不变量；#1029 新增「命令钩子与专用适配器的选择边界」一节，记录 `\global` 前缀被 `cmd/.../before` 钩子内赋值消耗的通用陷阱，以及 `\sbox`／`\savebox` 改用专用适配器重定义内部入口 `sbox ` 的原因，与 `color@b@x`／`@textcolor` 同属一类；#1043 新增「语法判断前必须消解参数里的对齐符」一节，记录在**对齐环境内**读取参数时 catcode-4 的 `&` 会终止 expl3 条件式正在读的宏参数（`&` 为 catcode 4 是 LaTeX 默认，非 `\halign` 所设；同一 token list 在对齐环境外 0 错误），须在 `_head:n`／`_tail:n` 这一层用 `\scan_stop:` 占位消解（而非逐个适配器修）；
  - 标点压缩（#975 对 #443/#481/#488 的预设与方向性修复，以及 #511 重构边界）和间距语义（#808）；
  - 兼容补丁（#510、#873/#880/#910/#919/#931/#972）、`\char` 约束及扩展子包；xeCJKfntef 部分现含 #1012 的接点分工：默认波浪与斜线使用随字号缩放的 `l3draw` `1em/4` 图案和普通 `\leaders` 共享相位，以首末局部裁切控制普通／带减号形式的可见范围，并在可断点两侧各放半周期连接；斜线约高 `.93em` 并下移 `.09em` 以覆盖常见汉字的高度和深度；空参数和不产生节点的正文不把 `ulem` 结尾语法空格变成可见装饰；自定义波浪符号保留 `\xleaders`；另含 #1017 对装饰盒使用空 `ActualText` 并在最小范围暂停 tagging 的 PDF 文本语义隔离，以及 #1026 的 ulem 集成层约束：`\UL@on`/`\UL@onin` 只在正文是“公式尾＋尾随源码空格”时才重排，其余情况必须保持字面记号，否则西文词右侧的 `\CJKecglue` 会被固定宽度装饰片段盒固化收缩量；宏承载正文属于发布版即有的既有限制，不属于该修复范围；#1037 补上同一机制的另一半（并含「同一根因共四处补 ecglue 的地方」一节、逐行插桩穷举法与显式分组写法的已接受限制）：西文词**前**的 ecglue 因源码空格被前视吃掉、marker 落在 `\UL@start` 刚开的片段盒内而固化，改经入口 `\@@_use_ecglue_skip:`（主体默认普通 `\skip_horizontal:N`，由 xeCJKfntef 改写为经 `\@@_ulem_glue:n` 输出），并记录三条约束：改写的守卫是「装饰 stream 活动」与「`\UL@start` 为 `\@empty`（片段盒确实打开）」两个条件的合取（`\xeCJK_if_ulem_patch:TF` 只比较 `\ ` 的含义；而状态布尔在公式路径不复位，两者单独都会误判并执行悬空 `\UL@stop`）、默认实现须留在主体、不能改用放裸 glue 的 `\@@_boundary_use_ulem_glue:n`；#1038 更正 Boundary class 触发条件的两处表述（XeTeX 判断字符类时会展开宏、`\protected` 无效，故宏展开后以显式 `{` 开头同样触发；`\bgroup`／`\egroup` 也**会**触发，此前长期记载为不触发——但原因是 catcode 1 不属于 letter/other 那几个分支，而非「被展开成花括号」），并把 Boundary 从可枚举清单改述为「下一个不可展开记号不是 letter/other 字符」的统称，并新增「最小吸收原则」一节说明 interchartoks 注入的代码只能吸收判断所必需的最少记号；#1057 为 `stream-ulem` 一节补上装饰命令的线型／符号型分类（线型借 `ulem` 扫描画连续线条：`\CJKunderline`、`\CJKunderdblline`、`\CJKunderwave`、`\CJKsout`、`\CJKxout`、`\CJKunderanyline`；符号型逐字放独立符号、不经扫描：`\CJKunderdot`、`\CJKunderanysymbol`）——它决定谁会占用扫描通道，也决定「只有两层都是线型时才走 `\UL@onin`」——并把此前只讲 glue、间距与 marker 的描述补上**用户可见后果**：`\UL@onin` 把内层正文整段装进刚性 `\hbox`、盒子内部不再留有断点，因此线型互嵌时内层正文无法断行（顺序无关，原生 `\uline` 嵌本包线型命令同样如此），任一层为符号型则断行正常；这是 `ulem` 的长期约束而非本包回归，手册 §3.6.1 给用户向说明与替代写法，回归由 `fntef-nest-linebreak01` 双向固定；#1067 修掉此前记为「已接受的限制」的第三处收缩量固化——显式分组包住西文词（`\CJKunderline{虚室 {hello} 生白}`）：花括号在片段盒内部才展开成分组，`\@@_ulem_glue:n` 的 group tag 守卫据此拒绝搬运（直接原因），但绕过守卫本身做不到（搬出来的 glue 仍落进 `\cleaders` 内部，且会让分组内字体设置丢失），修法是新增 `\@@_ulem_defer_glue:n`／`\@@_ulem_flush_pending_shrink:`，把间距拆成「盒内不可伸缩 kern 占自然宽度」加「全局记账，到词尾搬运处补一枚零宽带伸缩 glue」两半输出，守卫本身未改动；`fntef-shrink01` 的 TEST 11 覆盖三种花括号写法与 `\CJKglue` 零宽短路（换成 kern 会丢掉伸长量）。
- `llmdoc/architecture/ctex-architecture.md` — ctex 独立架构详解：分层加载、键值选项、引擎适配（含 pdfTeX UTF-8 `\DeclareUnicodeCharacter` 优先查找和 #381 `CJKfntef` 后端边界）、字号与字距系统（含 #871 `letterpress` 语义及 #402 `autoindent` 零缩进兼容边界）、方案/标题/字体集（含 #275 可展开的标题编号、完整标签与编号开关查询）、命令补丁，以及 Babel/biblatex 公开组合方法与运行时补丁的职责边界。
- `llmdoc/architecture/cleveref-patch.md` — cleveref 兼容补丁机制、挂钩链、`patch/cleveref` 开关与 Issue #725 根因分析。

## reference

- `llmdoc/reference/build-and-test.md` — `l3build`、共享构建配置、根 `Makefile` 本地任务入口（#888，含 `make changelog` #961）、`ctex` 185 个主回归测试，以及 #994 `fontset-macnew01` 对生成配置、macOS 实际字形和 SPA 度量的分层验证；还含 xeCJK #992/#1002 命令边界测试：直接输入 oracle、`00/10/01/11`、`xCJKecglue=false/true`、默认/可区分间距与状态归零断言；`command-boundary01` 当前执行 1668 个绿色单元，`command-boundary-math01` 执行 5504 次公式比较，`command-boundary-math05` 固定尾随空格的伸缩量和不可见节点边界；#1010 的 `boundary-register-api01/02` 另以 288 项行为比较和诊断回归固定公开注册入口；#1012 将 fntef 绘图验证分为真实图形度量、同尺寸轻量节点替身、XDV→无压缩 PDF→Lua 坐标校验、精确视觉 MWE 和整本文档集成构建，并要求核对实际加载的宏包路径；#1017 的 `fntef-actualtext01` 覆盖八类装饰的空 `ActualText` 与 tagging 暂停／恢复，并要求普通／tagged PDF 文本提取和页面视觉分别验收；#1026 的 `fntef-shrink01` 用主垂直列表真正断行固定 ulem 正文外层收缩量约束，并记录“单 `\hbox`/`\vbox` 抹平内外层区分”“调用处宏承载正文触发既有限制”两条测试设计反例；#1037 把该测试判据由「溢出恰为 3.64pt」改为「无 `Overfull`」（原判据把残留缺陷冻结成预期基线），并新增 TEST 6 用 `\badness` 正向断言外层可收缩量（记录 `\hbox to` 实际宽度恒等于目标宽度、不能作为收缩量判据）、TEST 7／TEST 8 固定守卫的两个象限、TEST 9 覆盖第三条补 ecglue 的路径并固定显式分组写法的已接受限制；#1029 的 `boundary-sbox-global01` 固定 `\global\sbox` 与内部入口加前缀两种写法跨分组保住内容（缺陷时退化为 0.0pt）、`\global\savebox` 跨分组仍为 0.0pt 这条上游既有限制、不带 `\global` 仍为局部赋值、嵌套 `\sbox` 的暂停深度显式归零，以及西文正文中藏 CJK 的 scratch box 不污染外层间距；每项使用独立 savebox，避免读到上一项遗留的全局值；#1046 的 `codedoc-meta-symmetry01` 用**真实 `l3doc` 类**以 12 项断言固定 `\meta` 两侧同源（并记录既有 `codedoc-meta-ecglue01` 因模拟内层函数时省掉 `\texttt` 而对该缺陷零判别力），#1047 的 `hyperref-anchor-ecglue01` 以 12 项断言固定 hyperref 行内锚点​**已覆盖**的三个出口 `\hyper@anchor`／`\Hy@raisedlink`／`\__hyp_target_raise:n`（三组判别力互不重叠即三处彼此独立的证据；第三处还区分「去掉包装」与「误用无花括号变体」两种失败形态——前者少 3.33pt，后者读数暴涨），并以 TEST 10 固定**已知未覆盖**的 `\hyper@anchorstart` 裸调用（`\pdfbookmark`）；文档只维护两份清单、不给出出口总数，因为那个数字连错四次；两者还固定三条测量类用例约束——一律用 `\newbox` 具名寄存器（被测命令会占用低位 scratch 寄存器，失败表现像实现缺陷）、字体预热要覆盖被测命令自己切换到的字形、不要用依赖 `.aux` 的量做宽度比较（`l3build` 只编译一遍，`\ref` 排出 `??`）。文档还新增「判断测试失败是否由本次改动引起」一节，给出「同一环境跑 master 并逐字节比对 diff 文件」的具体命令，以及 `l3kernel`／`l3backend` 版本错配使后端 special 变成可见文本这一环境指纹。#1057 新增 `fntef-nest-linebreak01` 以 6 个 TEST 双向固定「装饰命令嵌套时的断行」这条 `ulem` 既有限制（线型套线型的基线**含** Overfull 行、符号型参与的组合与手册替代写法的基线**不含**，两侧各做变异验证；把 TEST 3 内层换成线型后基线因多出 Overfull 行而失败），并借此给 `\UL@onin` 补上首个回归保护——原记载的「该路径无回归保护」由此改写为「`fntef-shrink01` 以外层收缩量覆盖 `\UL@on`、`fntef-nest-linebreak01` 以能否断行覆盖 `\UL@onin`」，但重排分支内部逻辑的正确性仍依赖代码审查；xeCJK 标准测试当前为 123／123（原 122／122）。文档还记录节点测试、显式 glue 的来源限制、`\kern0pt` 处理方法、字体预热、多引擎基线、CI/CD、版本检查和本地 TeX Live usertree 同步；agentic 工作流小节现描述 #1032 简化后的 Agent runtime：六个 Agent job 以 runner 默认用户运行并拥有完整本地执行权限、单脚本工具安装（`setup-agent-tools.sh`）、restore-only 缓存，保留只读 token／publisher 隔离／`review_status` 校验／Claude `--bare`／llmdoc `package-base` 重新检出，并说明为什么工具安装不再用复合 Action（#1030/#1031 的字段合法范围与 `pipefail` 规则仍适用于 `run-agent`/`feishu-notify`），以及 `pull_request_target` 可信 checkout 固定在 PR 分叉点（`base.sha`）而非 base 分支 HEAD 的语义；新增「xpinyin 的注音回归（#1041）」一节，记录 xpinyin 从零建立独立回归测试并接入版本校验的完整过程：引擎覆盖为何是 xetex + pdftex 且两者都必须跑（`\@@_adjust_xeCJK_hook:` 与 `\@@_adjust_CJK_hook:` 是两套互不复用的适配，luatex 被 `\msg_critical:nn` 明确拒绝）、为何必须分两个 `testfiledir`（`l3build check` 无按文件指定引擎的机制）、四条判别力教训（oracle 未切到候选同一字体族致全部单元恒报 DIFF、拼音字体缺字导致候选与 oracle 同时缺字的假通过、只测带数字的 v 漏掉 `\@@_replace_v:n`、`\xpinyin{长}{zhang3}` 恰好命中数据库首选值而无判别力）与两条结构性事实（注音汉字宽度看不出拼音内容——拼音在零宽 `\hbox_overlap_right:n` 盒里；CJK 环境必须开在盒子内部，否则宽高全为 0pt 让「宽度不变」假通过），以及 `\showbox`／`\box_log:N` 在 `-halt-on-error` 下抛 `! OK.` 当场中止须用 `\loggingoutput`、`checkdeps` 必须配 `checkinit_hook` 才能让测试用到工作树而非系统 TeX Live 的依赖包；「版本管理」覆盖矩阵表随之更新，xpinyin 从「无 version / ✗ / ✗」变为「`build.lua` version / 两处写法 / 共享 `update_tag` / ✓ / ✓」，并补一节仿 xeCJK 写法的发版 SOP；CI/CD 小节同步记录 `check-tag.yml`／`check-changelog.yml` 新增 xpinyin、`_test-package.yml` 的 `needs-unihan` 与 `configs` 输入，以及 `.github/tl_packages` 新增测试字体 `dejavu`／`gnu-freefont` 的维护约束；「版本管理」覆盖矩阵随 zhnumber／xCJK2uni 接入而拆细（两者从 ✗ 变 ✓；jiazhu 与 zhmetrics 各自单列，并注明 jiazhu 无 `jiazhu-v*` 触发器、发不出版故属潜在缺口），手工对账改由 `scripts/check-version-gate-coverage.py` 自动执行（接在 `check-tag.yml` 的 `gate-coverage` job，判据是「`.dtx` 里有没有 `l3build tag` 会回写的**三种**版本槽位」而非有没有 release tag／`version` 字段，核对四处接入点的交集，按可发版与否分级为硬失败或 notice，并如实记录它查不到「job 存在但被掏空」这条边界），另补两条坑：给某包补 check-tag job 必须同时加 `build.lua` 的 `version` 字段（否则不带参数的 `l3build tag` 打印「未指定版本号」并以 0 退出、diff 天然为零、job 恒绿却让矩阵显示 ✓），以及提取版本号的模式必须锚到行首结构标记（`zhnumber.dtx` 锚 `%<package|config>` 守卫；`xCJK2uni.dtx` 无守卫，只能锚行首空白加完整形状，借此排除该文件 `\date{...}` 里的同名 `\ExplFileDate`）；#1048/#1050 新增「上游宏包版本漂移的识别与基线处置」一节，记录 l3backend（tlnet 落后 CTAN 五个月，会自愈）与 pgf（`\pgf@sys@bp@correct` 舍入修正，不会回退）两个根因的版本坐标、TeX Live 打包元数据会滞后于实际文件内容这条判断依据、基线处置的分类判据（会自愈的不刷、上游不会回退的必须刷），以及「不能靠正则替换数字更新 `.tlg` 必须让 l3build 重新生成」「关掉断言不是刷基线」两条操作细节，并记录 CI 侧的临时 workaround——#1054 起已抽成共享脚本 `scripts/sync-l3backend.sh` 并接入三处（`_test-package.yml` 的 `Test <pkg>` 前、`_check-doc-package.yml` 的 `l3build doc` 前、`release.yml` 的 `l3build ctan` 前；`release-ctan-upload.yml` 只转发已有 zip、不重新排版故不接入），五步动作为比较 `\ExplFileDate` 与 backend 日期戳，不一致则从 CTAN 取 `l3backend`、逐 mirror 下载并**就地校验产物**（`-s` 非空加 `unzip -tq`）、docstrip 解包、装进 `TEXMFHOME` 后**无条件 `mktexlsr`** 让 kpse 看得见、最后核对 kpse 解析结果与日期一致，日期追平后自动降为空操作并打 `::notice::` 提示撤除（撤除判据即该 notice，要删的是脚本 + 三处调用 + 三处触发路径条目），并说明末尾核对不能是唯一防线（它把网络故障与索引陈旧一律报成「注入未生效」）；同一错配在 regression 路径表现为 `.tlg` 红、在 doc/ctan 路径不产生任何非零退出码而只在 PDF 正文留泄漏文本，判别方式分别是看 `.tlg` 与 `pdftotext` 后检索泄漏模式；`scripts/sync-l3backend.sh` 已进三处触发白名单（`check-doc.yml` 的 `on.paths` 与 `_all` filter、`test.yml` 的 `_all` filter），并记录两种失效机制（`on.paths` 不匹配则 workflow 不触发；`paths-ignore` 型会触发但各包 `if` 全 false 而整体跳过，故「看 job 有没有启动」不能作为门禁生效的证据）；「文档编译校验」一节记录 `verify-doc-output.sh` 的三条判据都是容器级、对「编译成功但正文被污染」零判别力（详见 `memory/doc-gaps.md`），以及 check-doc 在成功时也上传 `check-doc-<pkg>-pdf` artifact（只传 PDF 不传 log，保留期 90 天，与 TL／字体 cache 是独立配额）供目视检查；「本地测试失败的环境指纹检查表」补充两行指纹（`\cleaders`＋`\glue` 几何数值 diff 疑 backend/kernel 版本不匹配、`\special{pdf:btrans matrix ...}` 坐标末位变化对应 pgf 舍入修正）并把表头前提从「本地失败而 master CI 全绿」改为有条件的表述；`.github/tl_packages` 维护约束一节补充 TL 缓存 key 含 `hashFiles('.github/tl_packages')`、改动该文件等价于强制 cache 失效、master 的绿因此不能作为「代码在当前上游下仍通过」的证据；「本地 TeX Live usertree 同步」新增「往 check 环境注入替代版本的上游宏包（localdir）」一节，记录必须放 `localdir`（由 `checkinit()` 在 `l3build-check.lua:74-76` 复制进 `testdir`，直接写 `testdir` 会被 `cleandir` 清空、`TEXINPUTS` 也被 `:850` 写死的设置覆盖）、生效判据是核对测试目录里对应文件的日期戳、以及与 `checkinit_hook`（永久构建配置）在目标上的区分。#1067 新增 `fntef-shrink01` 的 TEST 11，修掉此前 TEST 9 记为「已接受的限制」的显式分组包住西文词一项（`braced-shrink-by-2pt-badness` 由 1000000 改为固定修复后的有限值），覆盖 `{hello}`／`\textbf{hello}`／`{{hello}}` 三种写法与 `\CJKglue` 零宽短路（该处若换成 `kern` 会丢掉伸长量，`fntef-font01` 盒宽随之漂移）；xeCJK 标准测试当前为 122／122。#1048/#1050 新增「上游宏包版本漂移的识别与基线处置」一节，记录 l3backend（tlnet 落后 CTAN 五个月，会自愈）与 pgf（`\pgf@sys@bp@correct` 舍入修正，不会回退）两个根因的版本坐标、TeX Live 打包元数据会滞后于实际文件内容这条判断依据、基线处置的分类判据（会自愈的不刷、上游不会回退的必须刷），以及「不能靠正则替换数字更新 `.tlg` 必须让 l3build 重新生成」「关掉断言不是刷基线」两条操作细节，并记录 CI 侧的临时 workaround（`_test-package.yml` 在 `Test <pkg>` 前比较 `\ExplFileDate` 与 backend 日期戳，不一致则从 CTAN 取 `l3backend` 装进 `TEXMFHOME` 并核对 kpse 解析结果，日期追平后自动降为空操作并打 `::notice::` 提示撤除，撤除判据即该 notice）；「本地 TeX Live usertree 同步」新增「往 check 环境注入替代版本的上游宏包（localdir）」一节，记录必须放 `localdir`（由 `checkinit()` 在 `l3build-check.lua:74-76` 复制进 `testdir`，直接写 `testdir` 会被 `cleandir` 清空、`TEXINPUTS` 也被 `:850` 写死的设置覆盖）、生效判据是核对测试目录里对应文件的日期戳、以及与 `checkinit_hook`（永久构建配置）在目标上的区分；新增「zhnumber 的计数器选项回归（#1008）」一节，记录 `counter-options01`（三引擎，记号层面）与 `legacy-entry01` + `test/config-cjk.lua`（仅 xetex，真排汉字量盒子）两个 `testfiledir` 的分工——兼容入口 `\zhnumwithoptions`／`\zhdigwithoptions` 不可展开，观察其行为只有「让它执行」一条路，而排 CJK 又要求按引擎分目录（pdfTeX 硬错误、XeTeX 仅警告）；记录四条走不通的观察手段（`\typeout`／`\tl_set:Nx` 只拿到命令名，`\TEST` 参数内切 `\ExplSyntaxOn` 不生效，`\protected@edef` 含 `@` 报错）与盒子度量选高度而非宽度的判别力实测（缺字时宽度 collapse 到同一值），以及如实记录的既有粗糙点（`\@@_counter_error:n` 的 `\???` 展开中报错，与 #1008 无关，未固定进基线）。xeCJK 标准测试文件数不变（仍 123／123），本次只在既有的 `fntef-shrink01.lvt` 内新增 TEST 11。「`ctex` 主测试目录当前覆盖面」的行距与间距一行补 `ccglue03.lvt`（#1068），并新增「`\selectfont` 重置用户汉字间距（`ccglue03`，#1068）」一节：记录 `\@@_update_stretch_auxii:` 那段 `\ctex_at_end:n` 重定义此前被 docstrip 守卫限定在 `%<*pdftex|xetex>`、luatex/uptex 因而缺守卫的根因，判据是解包产物里该重定义的出现次数（修好前 xetex 1 处、luatex/uptex 0 处，修好后五引擎均 1 处），以及 `ccglue03` 专测这两个引擎、既有 `ccglue01`／`ccglue02` 对它们 early-exit 从未覆盖过；同一节记录 `\ccwd` 连带变化非本次副作用（对照 `linestretch=\maxdimen` 既有路径）与 `linestretch` 不能作类选项这一未处理的既有限制；「`ctex` 新增回归测试的稳定技术模式」补第 8 条：测试文件里的引擎 early-exit 是覆盖缺口的标记，不是覆盖完成的证明。新增「zhnumber 的计数器选项回归（#1008）」一节，记录新增「zhnumber 的计数器选项回归（#1008，测试布局经 da00ad53 更正）」一节，记录xeCJK 标准测试文件数不变（仍 123／123），本次只在既有的 `fntef-shrink01.lvt` 内新增 TEST 11。
- `llmdoc/reference/coding-conventions.md` — expl3 命名、e-type 优先约定、`@@` 私有空间、`.choices:nn` 用 `#1` 替代 `\l_keys_choice_str`（#806 / #881）、catcode-class regex 的匹配优势与替换端 codepoint 局限（#378 / #879）、字面字符当 token 级替换模式时必须核对 catcode régime（含 `&` 为 catcode 4 的真正原因是 LaTeX 环境默认而非 `\c_code_cctab`／`\ExplSyntaxOn`、惰性 token 占位、探针先用已知答案自证、反向验证须对准被断言的机制，#1043；同一机制族在 `.lvt` 测试文件里的复现——`\TEST` 参数内部切换 `\ExplSyntaxOn` 不生效，因为参数读入时 catcode 已冻结）、会被写进辅助文件的 expl3 记号名字不能含 `_`（`_` 在 `.toc`／`.aux` 重新 tokenise 时不是 letter，写出那一刻就断名；判据是跑两遍编译而非一遍，#1008）、作用域语义（含用户可见命令全局/局部选择 #751 + 镜像分组局部原语状态的布尔标志必须同样局部 #431）、`.lvt` 空格规则及 `\ExplSyntaxOn` 宏定义中嵌入 Lua 时的空格与语句分隔、docstrip 标签、`\CTEX@` 遗留接口，以及 ctxdoc 对 l3doc 2026-06-18 的私有接口校验与 #963 长函数名压缩边界。- `llmdoc/reference/coding-conventions.md` — expl3 命名、e-type 优先约定、`@@` 私有空间、`.choices:nn` 用 `#1` 替代 `\l_keys_choice_str`（#806 / #881）、catcode-class regex 的匹配优势与替换端 codepoint 局限（#378 / #879）、字面字符当 token 级替换模式时必须核对 catcode régime（含 `&` 为 catcode 4 的真正原因是 LaTeX 环境默认而非 `\c_code_cctab`／`\ExplSyntaxOn`、惰性 token 占位、探针先用已知答案自证、反向验证须对准被断言的机制，#1043）、作用域语义（含用户可见命令全局/局部选择 #751 + 镜像分组局部原语状态的布尔标志必须同样局部 #431）、`.lvt` 空格规则及 `\ExplSyntaxOn` 宏定义中嵌入 Lua 时的空格与语句分隔、docstrip 标签与 `.dtx` 实现区的 `macrocode`／shortvrb 排版边界、`\CTEX@` 遗留接口，以及 ctxdoc 对 l3doc 2026-06-18 的私有接口校验与 #963 长函数名压缩边界。- `llmdoc/reference/coding-conventions.md` — expl3 命名、e-type 优先约定、`@@` 私有空间、`.choices:nn` 用 `#1` 替代 `\l_keys_choice_str`（#806 / #881）、catcode-class regex 的匹配优势与替换端 codepoint 局限（#378 / #879）、字面字符当 token 级替换模式时必须核对 catcode régime（含 `&` 为 catcode 4 的真正原因是 LaTeX 环境默认而非 `\c_code_cctab`／`\ExplSyntaxOn`、惰性 token 占位、探针先用已知答案自证、反向验证须对准被断言的机制，#1043）、作用域语义（含用户可见命令全局/局部选择 #751 + 镜像分组局部原语状态的布尔标志必须同样局部 #431）、`.lvt` 空格规则及 `\ExplSyntaxOn` 宏定义中嵌入 Lua 时的空格与语句分隔、docstrip 标签、`\CTEX@` 遗留接口，以及 ctxdoc 对 l3doc 2026-06-18 的私有接口校验与 #963 长函数名压缩边界。
- `llmdoc/reference/kpse-path-resolution.md` — kpse 的文件查找语义（#1054 拆出）：回答「文件拷进某棵 texmf 树之后 kpse 能不能看见」。记录 `TEXMFDBS = {!!$TEXMFLOCAL,!!$TEXMFSYSCONFIG,!!$TEXMFSYSVAR,!!$TEXMFDIST}` 与 `TEXMF` 列表里 `$TEXMFHOME` 无 `!!` 前缀、`!!` 的语义是只查 ls-R 绝不扫磁盘（实测该树下磁盘上真实存在的文件无 ls-R 条目时 `kpsewhich` 完全找不到）、无 `!!` 的树有「ls-R 比目录旧就回退扫盘」的宽容行为、`mktexlsr` 在没有 ls-R 的树上会新建一个；由此得出反直觉后果——刷新 ls-R 会关掉扫盘回退，所以「刷过索引」的那个 job 反而找不到随后拷入的文件（`_check-doc-package.yml:251` 的 zhmetrics tfm/map 与 `scripts/sync-l3backend.sh:113-128` 的组合就是实例）；另记本地 `TEXMFHOME` 通常是普通树而 CI 上 setup-texlive-action 让它解析到带 `!!` 的 `texmf-local`，因此涉及 usertree 可见性的问题**本地默认不具备复现前提**，应写最小独立复现直接测机制本身。
`counter-options01`（三引擎，记号层面，末项断言 `\zhnum` 可展开报错）／
`counter-options02`（三引擎，专门断言 `\zhdig` 那条独立守卫的同类报错）／
`legacy-entry01` + `test/config-cjk.lua`（仅 xetex，实际排出汉字再量盒子，覆盖不可展开的
兼容入口）三个文件的分工；记录本节最重要的约束——`\@@_counter_error:n` 的
`Use of \???...` 在本仓库是致命错误、会让编译当场中止（成因是
`support/build-config.lua:9` 的 `checkopts = "-halt-on-error"`，不是 l3build 或 LaTeX
的默认行为；这是 #1026 用 `\showbox` 撞过的同一机制的第二个触发源），因此在本仓库
**一个 `.lvt` 只能断言一次可展开报错且必须放在文件最末**，报错文本本身必须
固定进基线（代价是 `counter-options01` 需三份基线、`counter-options02` 需两份，
luatex 的 help 行比 xetex/pdftex 少四行），验收
判据是「基线里的 TEST 段落数与 `.lvt` 里的 `\TEST` 个数一致」；记录四条走不通的
观察手段（`\typeout`／`\tl_set:Nx` 只拿到命令名，`\TEST` 参数内切 `\ExplSyntaxOn`
不生效，`\protected@edef` 含 `@` 报错）、盒子度量的两次判别力踩坑（缺字
时宽度 collapse 到同一值，故改用高度；但高度同样不足以固定字形——「柒」「九」「佰」的
`ht` 都是 7.75，最终由 `legacy-entry01` 的 TEST 4 用
`\loggingoutput` + `\box_use:N` + `\clearpage` 把六个入口在一位数和多位数两种取值下
的盒子内容本身写进基线，度量只作旁证），以及主目录里对兼容入口的 `\tl_set:Nx` 断言恒真（因
protected）已删除、完全由 `legacy-entry01` 覆盖；`check-changelog.yml` 一节同步
补充新增的占位符校验（`CHANGELOG.md` 不得含 `extract-changes.py` 的 `\x00`–`\x05`
占位符，与既有新鲜度校验互补而非重叠——占位符漏出是确定性的，两边生成物一致只是
都错，新鲜度 diff 零判别力）。
- `llmdoc/reference/ctex-fontset-mac.md` — `ctex` 中 `fontset=mac` / `macnew` / `macold` 的选择逻辑、macOS 15+ 检测后备、XeTeX/LuaTeX 字体探测差异与回退语义，以及 #994 更换正文常规字形时各后端映射和 SPA 数据的同步清单、平台专属回归的证据边界。
- `llmdoc/reference/repo-git-conventions.md` — 仓库级 git 约定：CODEOWNERS 默认与 zhlineskip 专属审查归属、pre-push self-wrapper 的真实 push/CI/review 状态判定、bot 评论由维护者证据回复确认后的无空提交终止路径，以及长期 orphan 分支 `gh-assets` 的资产组织、安全写入和迁移收尾（现含 #275/#402、#995/#996/#998 等 MWE 与对比图）。

## guides

- `llmdoc/guides/push-and-pr-review-workflow.md` — 安装 self-wrapping pre-push、无管道执行 push、解读内层 push 与 rc、联合审计 GitHub review 和被 git 忽略的本地 `.code-review` 报告；正式本地审查必须启动不继承主对话的新子代理，只提供仓库规则、公开目标、完整 base/head SHA、范围和必要公开证据，并在报告中记录隔离方式与允许输入；另含隔离增量审查、bot finding 确认、按风险验证、复杂排版 PR 图示、新分支首次 PR 补跑及 llmdoc 收尾；现含 Agent job 在可信 checkout 或安装阶段失败时先核对 PR 分叉点、用 rebase 而非 close/reopen 恢复的诊断顺序。
- `llmdoc/guides/release-workflow.md` — 两阶段 release 流程: ① `release.yml` 推 tag 自动打 CTAN zip + 发 GH prerelease(公测); ② `release-ctan-upload.yml` 手动触发, 复用同一 zip + LLM 忠实翻译 `scripts/extract-changes.py` 抽出的 release notes 为英文 announcement 投递 CTAN, 成功后翻 GH Release 为 latest; `announce=false` 可跳过 announcement; 本地 `make tag` 打 release tag; 含 `scripts/extract-changes.py` 参数语义(单版本模式字节兼容承诺 + `all`/`-o` 参数 #961); 流水线编号步骤现含第 6 步 `Sync l3backend to l3kernel`（#1054，`l3build ctan` 之前必须跑：ctan target 内部会 call check，`.tlg` 侧的红能发现，但它同时打包 typeset 出的 PDF，正文泄漏不触发任何退出码，缺这一步会把带缺陷文档送进 CTAN zip；`release-ctan-upload.yml` 只转发 zip、不重新排版故不接入）。

## memory

- `llmdoc/memory/reflections/xecjk-dtx-documentation-boundaries.md` — 反思：`.dtx` 同时是程序源和排版源，docstrip 产物可用不能证明手册结构正确；`support/ctxdoc.cls` 对 `%    \end{macrocode}` 的四空格终止行做逐字匹配，错一格会在远处表现为 quark 错误；`\changes` 中的参数签名和长内容不能机械使用 shortvrb，须按内容选择语义宏并以完整文档构建验证。
- `llmdoc/memory/lessons-learned.md` — 从已归档反思提炼的跨任务规则；当前含正式审查的新会话、受限输入和报告留痕校验、生成物校验、按风险收缩验证、平台条件测试的陈述边界、字体选择／映射／度量同步、节点/视觉证据、周期装饰的接点分工、源码树实际加载路径核对、PDF 绘图的尺寸／节点／坐标／外观分层回归、稳定文档随实现演进复核、源码候选须由实际输出确认、弹性 glue 的三项参数与实际断行（含单 `\hbox`/`\vbox` 抹平内外层区分的具体反例）、回归测试须以重新引入缺陷的方式确认会失败、命令边界输出等价矩阵、绿色单元与失败基线分离、机制抽象与支持边界、说明层隔离、字体预热、PR 预览与主线事实分层、feature request 和 git 测试规则、复合 Action 与 job step 字段/默认值语义、加载期失败遮蔽运行期缺陷的 CI 规则，新增的一组「修复到位判定与断言强度」规则（三对照点含未受影响发布版、回放报告者原始 MWE 而非自己基线、`\hbox to` 宽度恒真不可作判据须用 `\badness`、通用路径不得引用可选子包函数、探针静默先自证有效、装饰改动须同时验收节点与像素、复用带守卫函数须重验其在新调用点的前置条件、「加载子包但不用该功能」是独立验证象限），新增「「跑了但什么也没校验」的 job 比没有 job 更危险」与「对账／校验脚本本身也要做判别力实测」两条（前者源于 zhnumber／xCJK2uni 接入时未设 `version` 致 check-tag job 恒绿，后者源于对账手段两次犯下与被查问题同型的白名单错误），并给「白名单式 CI 校验默认放行」补上第二次实例与自动对账的落实方式；以及「LaTeX2e 命令钩子机制」一组规则：通用命令钩子不能包装命令本体即赋值语句的场景、报告者变通不能替代对代码历史用途的核实、缩小复现到不含本包最小样例才能确认是上游通用陷阱；以及 #1046／#1047 新增的五条证据规则（含穷尽性断言与成因断言的规则，及「反复出错的穷尽性断言改为维护已覆盖／已知未覆盖两份清单」）：既有测试全绿只说明测试覆盖的场景没问题（用简化替身模拟被测对象时，简化掉的那一层可能正是缺陷所在）、否定性结论要说明搜索了什么模式与为何能穷尽、现象、联系、穷尽性、成因是四个独立命题（「A 缺了会坏、B 缺了也会坏」既不证明「按某条件在 A、B 间分派」也不证明「只有 A 和 B」，观察到故障也不证明你对成因的解释；控制流用计数器、穷尽性要说明如何排除下一种、成因用隔离实验）、测量类用例一律用具名 box 寄存器（被测命令会占用低位 scratch 寄存器且失败表现像实现缺陷）、判断测试失败归属要在同一环境跑 master 并逐字节比对 diff 文件（`tlmgr` 报无更新不等于本地各包自洽），字体预热规则也补上「要覆盖被测命令自己切换到的字形」；新增一组「本机/CI 环境漂移与上游基线处置」规则（#1048/#1050）：本机能复现推不出 CI 会复现、CI 缓存分叉时某一侧的绿不构成判据、刷 `.tlg` 基线前先按上游根因分类（会自愈的不刷／上游不会回退的必须刷）、关掉断言不是刷基线、注入类实验必须有可核实的生效判据、排查上游问题前先查本仓库已有根因记录；#1054 又补一组：kpse 能不能看见文件取决于那棵树有没有 `!!`（刷索引反而关掉扫盘回退，且这类问题本地默认不具备复现前提）、命令退出码为 0 不等于产物合格（失败要暴露在发生处，否则末尾总校验会报出与真因无关的原因）、抽出共享脚本时触发白名单与 job filter 属于「调用点」的一部分（两种失效机制不同，「看 job 有没有启动」不是门禁生效的证据）、对照实验不要用 `sed`／`perl` 删真实脚本片段（应改用最小独立复现直接测被怀疑的机制）、`\changes` 里的 makeindex 特殊字符要按 `.gls` 与 CHANGELOG 两个下游同时验收、看似笔误的转义字符先查工具链语义；另有两条既有条目被扩写——「注入类实验必须有可核实的生效判据」增补否命题形态（反证得到「仍然失败」或「未复现」时，先证伪「装置无效」与「环境不具备复现前提」再采纳结论），「排查上游问题前先查本仓库已有根因记录」的适用范围扩到「写记忆／文档之前也要先查 llmdoc 是否已有该内容」。#1057 又新增四条：「症状不显现」不等于「路径不可测」，换一个可观察量再判断（说某路径不可测时实际说的是「当前观察量在那里恒定」，那是观察量的性质；候选观察量的判据是它是否依赖那段代码改变了的结构——受结构影响的量是候选、被该结构屏蔽的量不是；记为「不可测」时要写清当初的观察量与理由，并把「换观察量」列为待试项）、把既有限制固定成断言需要双向判据（触发与不触发两侧都要有断言并各做变异验证，否则不触发那侧只是空基线的默认结果，文档里的适用范围无人校验）、文档里的肯定性清单必须声明它保证的是哪一个维度（「以下命令行为正确」会被读成「以下命令没有问题」，相邻维度有已知限制要在同一处交叉引用；这类问题没有任何测试能拦住）、报告者的复合样例不能用来划定支持边界（必须自己做一次只变一个变量的控制实验，documentation 类 issue 上尤其要紧）；「从源码树验证时必须核对实际加载文件」一条同时扩写了成因——`TEXINPUTS` 未设或指向的目录不存在时 `xelatex` 不报错不警告，静默回落到系统同名宏包且读数照样像样，这条失效没有任何主动信号，不能靠「出错会提醒我」兜底（#1057 是它在 #1012、#1026、#1047 之后的又一次发作）。#1008 新增三条：「观察不可展开命令的行为只有让它执行一条路」（`\typeout`／`\tl_set:Nx`／`\protected@edef` 都只能拿到命令名，与 #1043「探针先自证有效」同属一类）、「聚合度量选哪一维要先验证它真的会随被测行为变化」（缺字时宽度可能 collapse 成同一值，须先用两个已知不同输入测一次候选维度）、「手册措辞会塑造用户对是否设计限制的判断」（报告者被「不可展开」一句引导到放弃尝试，修代码后要回头检查手册措辞是否仍会误导）；测试文件里的 catcode régime 一次定好这条已并入 `coding-conventions.md` 既有的 #1043 小节，不在本文件单独立条。测试文件里的 catcode régime 一次定好这条已并入 `coding-conventions.md` 既有的 #1043 小节，不在本文件单独立条；da00ad53 后 #1008 又新增三条：「可展开报错可能是致命的，断言须放文件末尾且核对基线段落数」（会中止编译的断言之后的用例不会执行而测试仍全绿，验收要比对基线段落数与 `\TEST` 个数）、「「生成物与源同步」和「生成物本身正确」是两个独立命题」（新鲜度校验对确定性缺陷零判别力，`extract-changes.py` 的占位符漏出即是一例）、「写下一条教训不等于已经把它应用到手头的产物上」（写下「观察不可展开命令只有让它执行一条路」后仍在同一测试文件里留了一处犯同样错误的 `\tl_set:Nx` 恒真断言）。da00ad53 后 #1008 又新增三条：「核对基线里的用例数与文件里的用例数」（并入既有的 `\showbox` 那条规则、按「`-halt-on-error` 下任何抛错的断言」而非按具体命令记；可展开报错是 `\showbox` 之外的第二个触发源，验收要比对基线段落数与 `\TEST` 个数，并注明成因是本仓库的 `checkopts` 而非 l3build 默认行为）、「「生成物与源同步」和「生成物本身正确」是两个独立命题」（新鲜度校验对确定性缺陷零判别力，`extract-changes.py` 的占位符漏出即是一例）、「写下一条教训不等于已经把它应用到手头的产物上」（写下「观察不可展开命令只有让它执行一条路」后仍在同一测试文件里留了一处犯同样错误的 `\tl_set:Nx` 恒真断言）。#1068 又新增两条：「缺陷按代码路径分布，不按报告者用的引擎分布」（报告只提 LuaTeX，实测 upTeX 同样受影响；是「测试结论不能超出实际执行的平台分支」#994 在缺陷侧的镜面）、「发现『某处缺少某个检查』时先在全仓搜索该检查是否已在别处存在」（第一版分析把「守卫调用点缺失」误诊成「守卫实现缺失」，据此设计了在 kernel 层新写等价逻辑的方案，`grep` 才发现三引擎的判断逻辑早已存在于 engine 层、只是被 docstrip 条件挡住；是「排查上游问题前先查本仓库已有根因记录」从「查文档」到「查代码实现」的推广）；并给「白名单式 CI 校验默认放行」补上测试文件层面的同构实例（既有 `ccglue01`／`ccglue02.lvt` 对 LuaTeX／upTeX 打 `not tested yet.` early-exit，恰好跳过了 #1068 唯一会坏的两个引擎）。可展开报错是 `\showbox` 之外的第二个触发源，验收要比对基线段落数与 `\TEST` 个数，并注明成因是本仓库的 `checkopts` 而非 l3build 默认行为）、「「生成物与源同步」和「生成物本身正确」是两个独立命题」（新鲜度校验对确定性缺陷零判别力，`extract-changes.py` 的占位符漏出即是一例）、「写下一条教训不等于已经把它应用到手头的产物上」（写下「观察不可展开命令只有让它执行一条路」后仍在同一测试文件里留了一处犯同样错误的 `\tl_set:Nx` 恒真断言；该规律又复发一次——更正一处错误归因时改了三份文档却漏掉该说法最原始的出处，故做法改为先 `grep -rn` 列全部出现位置再逐个改）、「测试输入的取值本身要能区分被测的各条路径」（一位数下 `\zhnum` 与 `\zhdig` 输出恒等，把 digits 路径接成整数路径的变异零判别力，须用 `123` 一类多位数）。da00ad53 后 #1008 又补入以下几条（不给总数：这份清单在盲审逐轮推进中反复增补，写死数字必然过期，见「改一处计数后用 grep 扫全部变体」那条规则）：「核对基线里的用例数与文件里的用例数」（并入既有的 `\showbox` 那条规则、按「`-halt-on-error` 下任何抛错的断言」而非按具体命令记；da00ad53 后 #1008 又补入以下几条（不给总数：这份清单在盲审逐轮推进中反复增补，写死数字必然过期，见 lessons-learned 里「事实性陈述的更正以全仓所有实例为单位」那条规则）：「核对基线里的用例数与文件里的用例数」（并入既有的 `\showbox` 那条规则、按「`-halt-on-error` 下任何抛错的断言」而非按具体命令记；#1008 新增三条：「观察不可展开命令的行为只有让它执行一条路」（`\typeout`／`\tl_set:Nx`／`\protected@edef` 都只能拿到命令名，与 #1043「探针先自证有效」同属一类）、「聚合度量选哪一维要先验证它真的会随被测行为变化」（缺字时宽度可能 collapse 成同一值，须先用两个已知不同输入测一次候选维度）、「手册措辞会塑造用户对「是不是设计限制」的判断」（报告者被「不可展开」一句引导到放弃尝试，修代码后要回头检查手册措辞是否仍会误导）；

- `llmdoc/memory/decisions/repo-push-hook-discipline.md` — 决策: 常规 push 必须以无管道命令完整运行 self-wrapping pre-push，按内层 push、CI 和 review 活动输出闭环修复全部问题；无需代码改动的 bot finding 用维护者证据回复确认，避免空提交触发无限 review 循环，并以 llmdoc 更新收尾。
- `llmdoc/memory/decisions/275-heading-query-interfaces.md` — 决策: 为 Beamer 等下游提供可展开、按层级查询裸编号、完整本地化标签和 `numbering` 状态的公共接口；不新增 Beamer insert 命令，也不公开样式私有宏。
- `llmdoc/memory/decisions/402-autoindent-zero-exception.md` — 决策: 保留 `autoindent` 对零 `\parindent` 不执行字号跟随的兼容语义，以保护旧文档和 `minipage`、居中段落等结构；补手册与四引擎回归，不改实现。
- `llmdoc/memory/decisions/158-165-jamo-cj-interchar-classes.md` — 决策: Hangul 用 L/V/T 转移区分音节内 shaping 与音节间 CJKglue；日文 CJ 默认 normal、可选 strict 独立类禁则，并保持 fntef 专用转移。
- `llmdoc/memory/decisions/271-varioref-chinese-upstream-locale.md` — 决策: 中文 varioref 本地化优先以上游 `varioref` 的 `chinese` locale 实现，先推动 latex2e PR #2071，而非在 ctex 侧维护整套描述性文本补丁。
- `llmdoc/memory/decisions/725-cleveref-patch-toggle.md` — 决策: 不在 ctex 侧修复 cleveref appendix 语义问题，改为提供 `patch/cleveref` 开关。
- `llmdoc/memory/decisions/751-newCJKfontfamily-scope.md` — 记录 #751 / PR #773 中 `\newCJKfontfamily` 从全局命令定义改为局部定义的原因、决策与影响范围。
- `llmdoc/memory/decisions/782-fontset-mac-macos15plus-detection.md` — 决策: 不新增 `mac15plus`，改为在 `fontset=mac` 内增加 macOS 版本检测后备，并按 XeTeX/LuaTeX 分别探测 macOS 15+ downloadable 字体。
- `llmdoc/memory/decisions/746-remove-legacy-font-hooks.md` — 决策: 移除对 LaTeX < 2020/10/01 的字体钩子兼容代码，响应上游移除 `\@rmfamilyhook`。
- `llmdoc/memory/decisions/688-pifont-interchartokenstate-leak.md` — 决策: pifont hook 中先进入水平模式防止 interchartokenstate 泄漏到输出例程。
- `llmdoc/memory/decisions/715-hyperref-driverfallback.md` — 决策: hyperref driverfallback 按加载状态分支处理，避免重复设置警告。
- `llmdoc/memory/decisions/717-experiment-cjkecglue.md` — 决策: 将跨引擎 `CJKecglue` 统一接口保持为实验性，并固定在 `ctex / experiment` 子路径下。
- `llmdoc/memory/decisions/543-font-size-system.md` — 决策: 将字号系统切换能力放入 `experiment/font-size-system`，仅在类/宏包选项阶段选择 `word`、`letterpress` 或用户自定义字号表；#813 将原名 `traditional` 更名为 `letterpress` 以明确其金属活字语义。
- `llmdoc/memory/decisions/761-ccglue-override.md` — Issue #761 CJKglue 导言区覆盖问题的修复方案演进与确立的引擎延迟重定义模式。
- `llmdoc/memory/decisions/811-halfright-prebreakpenalty.md` — 决策: #811 对整个 `HalfRight` 类施加条件禁则，不拆分类；其中 `FullRight -> HalfRight` 必须覆写 interchartoks 以保证 penalty 位于 punct glue 之前；"固定 13 个字符"仅为 #811 落地时静态基线，#431 起 `HalfRight` 成员随 `LatinPunct` 开关动态增减。
- `llmdoc/memory/decisions/826-fntef-right-side-cjkglue.md` — 已被 #999 替代的历史决策：旧 fntef pending/filter 由 `stream-ulem` 与统一 source-space 检查吸收，外侧 glue 由 framework 决定并排到装饰区间外。
- `llmdoc/memory/decisions/826-fntef-color-global-state.md` — 已被 #999 替代的历史决策：装饰符号 hbox 的手工状态保存/恢复改由可嵌套 capture suspend/resume 统一隔离。
- `llmdoc/memory/decisions/830-color-wraps-ulem-last-node-tl.md` — 已被 #999 替代的历史决策：ulem 不再保存并覆盖全局末状态，而是把可信末尾 marker 移到外层后由 stream capture 解释实际末类别。
- `llmdoc/memory/decisions/831-boundary-explicit-brace-ecglue.md` — 已被 #999 替代的历史决策：显式分组保留基础 source-space 门控；`\mbox`、color/xcolor 分别进入 box、transparent/wrapped-box 注册，颜色专用 pending 与通用 hlist/whatsit 猜测已删除。
- `llmdoc/memory/decisions/873-880-fixed-point-vs-default-narrowing.md` — 已被 #999 替代的历史决策：`\HD@target` 使用 transparent capture，完整 `\Url@z` 与 codedoc/doc meta 使用 stream；旧 save/replay 与 drain 均已删除。
- `llmdoc/memory/decisions/910-verb-drain-vs-drain-verb.md` — 已被 #999 替代的历史决策：`\verb` 使用 auto stream，旧 drain helper 已删除；#919 的 language whatsit 主动落盘只保留节点时序职责。
- `llmdoc/memory/decisions/931-biblatex-let-shadow.md` — 部分保留的历史决策：最终 `\let` 目标 `\blx@pagetracker` 与 preamble 末安装时机仍有效；旧单向 clear/whatsit 恢复算法已由 transparent capture 取代。
- `llmdoc/memory/decisions/935-check-doc-vs-ctan.md` — 决策: #935 新增 PR 校验 `check-doc.yml` 用 `l3build doc` 而非 `l3build ctan`。后者内部硬编码调 `l3build check` (整套 regression) 与 test.yml 完全重复; 前者是纯 typeset (docinit + typesetpdf), 精确对应"文档 dtx→PDF 可编译性"维度. 牺牲 tdslocations 打包路径验证 (低频问题, release.yml 兜底).
- `llmdoc/memory/decisions/937-version-single-source-l3build-tag.md` — 决策: #937 版本管理收敛为 build.lua 单一事实源 + `l3build tag` 回写 dtx `$Id:$` stamp + 两道 CI 校验 (check-tag.yml PR 校验 + release.yml 三方校验 strip_rc(git tag)==build.lua==stamp)。update_tag 必须幂等 (stamp 版本相等即跳过, 否则回写追着 commit 跑永不收敛); RC 后缀只存在于 git tag, 发 rc 前 build.lua 必须已 bump 并 stamp。适用 zhlineskip / ctex, 其他包迁移路径已列出；同一原则延伸到手册首页页脚 shorthash——曾短暂采用编译时 `\sys_get_shell` 现场取 git hash (`--shell-escape`), 四环境实测在 CTAN 典型解压场景导致 Emergency stop 被否决, 改为 `update_tag` 在 `l3build tag` 阶段固化写入 `ctex.dtx` 的 `\GetFileId[<hash>]`。
- `llmdoc/memory/decisions/961-changelog-gate-no-write-perm.md` — 决策: #961 CHANGELOG.md 生成物新鲜度校验收敛为「CI 每 PR 重新生成 + `git diff --exit-code` 只校验不回写」，与 #937 check-tag.yml 同一「生成物新鲜度校验」架构模式；否决 CI 打 tag 时生成并 commit（需 write 权限）与 tag 前本地手跑（流程不闭环）两案。已接受缺憾: zhmetrics 包名前缀推断为 `zhmCJK` 导致版本链接死链、并行 PR 的 CHANGELOG 合并冲突。
- `llmdoc/memory/decisions/972-hyperref-end-annot-trusted-marker.md` — 已被 #999 替代的历史决策：顶层 `\Hy@EndAnnot` 的末尾 math 仍作为 Default 观察点，但从 `\Hy@BeginAnnot` 到结束端的 auto stream 已取代专用 marker。
- `llmdoc/memory/decisions/991-setref-null-marker-replay.md` — 已被 #999 替代的历史决策：保留 `\null` 的目标不变，但 `\@setref` / `\real@setref` 已改用 auto stream，一般 `\null` 走 post-transparent；专用 wrapper、saved-node 与 replay 已删除。
- `llmdoc/memory/decisions/992-command-boundary-capture-register.md` — 决策：#992/PR #999 用 box、wrapped-box、stream、transparent、post-transparent 五种注册策略和运行时首尾类别统一恢复命令两侧间距；记录嵌套、`\sbox` 状态、旧逐命令补丁的替代结果、TeX 无法区分某些显式 glue 与源码空格的限制，以及 `\kern0pt` 处理方法。现含 PR #1001 对 #995/#996/#998/#1000 的修复、PR #1005 对 #1003 的外层 `spacefactor` 同步和有界后缀移动、#1002 的公式 oracle 与弹性间距状态，以及 #1010 实验性公开入口与内部框架、专用参数适配器之间的边界；#992 活表已从 PR #1005 的合并提交复验并更新。
- `llmdoc/memory/decisions/1002-inline-math-boundary-oracle.md` — 决策：行内公式命令只去掉外层包装，以直接公式而非西文字母为 oracle；保留 `xCJKecglue=false/true` 对公式旁源码空格的既有语义，并以“所有尾部公式语法只产生候选＋正文排完时的实际节点确认”避免把未知宏消费的分组、`$` 或 `\)` 误认成可见公式；现含 `math-space`／`math-space-frozen` 对参数内尾随空格的分流、不可见节点导致 marker 过期时的 glue 与盒子顺序、ulem 确认时机、5504 次宽度比较和节点回归。
- `llmdoc/memory/decisions/1010-boundary-register-public-api.md` — 决策：通过 `experiment/boundary-register` 向专家用户开放五种命令边界注册策略；`command` 直接接收控制序列，声明全局收集并在导言区末尾应用，普通 `AtBeginDocument` 后检查存在性；接口只新增、不反注册或覆盖，并在保存和应用两个阶段查询通用注册表与专用适配器保留表的并集。文档同时明确 mode 规则、特殊扫描器和公式参数适配限制。
- `llmdoc/memory/decisions/1012-fntef-decoration-overlap.md` — 决策：默认波浪与斜线使用随字号缩放的 `l3draw` `1em/4` 图案和普通 `\leaders` 共享相位；首末局部 PDF 裁切分别给普通／带减号形式提供对称的外伸／内缩范围，可断点两侧的半周期连接保持不断行与换行两种情况的接点语义；末段状态按嵌套层压栈和恢复；空参数和不产生节点的正文保持零尺寸；斜线约高 `.93em` 并下移 `.09em`，自定义 `underwave/symbol` 保留 `\xleaders`。
- `llmdoc/memory/decisions/1017-fntef-pdf-text-semantics.md` — 决策：xeCJKfntef 的字符型装饰盒统一使用空 `ActualText`，tagged PDF 在最小范围暂停 tagging；不依赖 Artifact，也不为文本语义问题改写 `l3draw`，并明确 #1012 的线条重叠不在本次范围内。
- `llmdoc/memory/decisions/1026-ulem-literal-body.md` — 决策：`\UL@on`/`\UL@onin` 按正文语法分派，只在“公式尾＋尾随源码空格”时用 `\@@_boundary_ulem_math_tail_space:nnn` 重排（保留 #1002 修复），其余情况保持字面记号，避免西文词右侧的 `\CJKecglue` 被固定宽度装饰片段盒固化收缩量；否决直接回退引入提交 `494d5a72`，因为它同时是 #1002 修复的一部分；已接受宏承载正文（如 `\CJKunderline{\BODY}`）触发同一限制，判断依据是发布版本对该写法同样得到修复前的溢出宽度。
- `llmdoc/memory/decisions/1032-agent-runtime-simplification.md` — 决策：#1032 删除 #1025 引入的三层进程隔离（专用用户、模型代理、控制进程加固），改回上游模板的 runner 默认用户执行形态，工具安装从复合 Action 改为单个脚本；否决理由是三层隔离没有对应威胁模型收益，且自身是三次连环故障（`timeout-minutes`、`pipefail` 下 awk 提前 `exit`、工作区所有权移交后 Agent 进不去）的唯一来源；已接受风险是模型 API key 暴露给 Agent 可执行的仓库代码，重新引入隔离的条件是开始接受 fork PR 自动审查或贡献者结构变化。
- `llmdoc/memory/decisions/1029-sbox-adapter.md` — 决策：#1029 不采纳报告者「删除 `cmd/sbox/before`／`after` 两个钩子」的变通，因为这两个钩子是 #992 系列刻意引入的、用于隔离 `\sbox` 内部 scratch box 测量的 capture suspend/resume，删除会撤销这层隔离；改用专用适配器 `\@@_boundary_sbox:Nn` 直接重定义内部入口 `sbox `，把暂停观察移到盒子构造内部，使 `\global` 前缀始终紧邻 `\setbox`，与 `color@b@x`／`@textcolor` 专用适配器同属一类；记录受影响命令范围（`\sbox`／`\savebox` 四种形式，含 picture 形式）和 `experiment/boundary-register` 的 `command` 策略对「命令本体即赋值语句」场景的同类风险与已加的手册警告。
- `llmdoc/memory/decisions/1041-xecjk-version-gate.md` — 决策：#1041 把 xeCJK 接入 `l3build tag` 与两道校验版本校验。`xeCJK/build.lua` 新增 `version` 作事实源；`support/build-config.lua` 的共享 `update_tag` 改为 `version` 优先（判类型，因 l3build 自带全局 `function version()`）、加幂等守卫、并修掉六个共用包裸跑 `l3build tag` 的 nil 报错；`check-tag.yml` 增 `tag-xecjk` job（`paths` 含 `support/build-config.lua`，diff 只限本包目录）；`release.yml` 增 `xeCJK)` case 校验 git tag / `version` / `{\ExplFileDate}{...}` 三方一致。记录背景（`v3.10.5-rc2` 发出了自报 `v3.10.4` 的包，两处白名单都未覆盖 xeCJK）与未做的白名单对账机制。
- `llmdoc/memory/decisions/1041-xpinyin-test-adoption.md` — 决策：xpinyin 从零建立独立回归测试并接入版本校验（#1041 后续，宏包代码本身未改动）。两条互不复用的适配路线（`\@@_adjust_xeCJK_hook:` 与 `\@@_adjust_CJK_hook:`）必须分别测试且必须分 `testfiledir`（`l3build check` 无按文件指定引擎的机制）；四个测试文件按观察通道（尺寸比较、`\loggingoutput` 节点列表、改前改后差值）而非功能模块分工；记录四条判别力教训（oracle 未同字体族、拼音字体缺字假通过、只测带数字的 v 漏掉 `\@@_replace_v:n`、`\xpinyin{长}{zhang3}` 命中数据库首选值无判别力）与两条结构性事实（注音宽度看不出拼音内容、CJK 环境必须开在盒子内部）；`\showbox`／`\box_log:N` 在 `-halt-on-error` 下抛 `! OK.` 须用 `\loggingoutput`；`checkdeps` 必须配 `checkinit_hook` 才能让测试用到工作树版本而非系统 TeX Live；版本管理沿用 xeCJK 的共享 `update_tag` 路线，`release.yml` 的 `xpinyin)` case 同时校验 dtx 两处版本写法。
- `llmdoc/memory/decisions/1046-1047-registration-point.md` — 决策：#1046／#1047 都不是策略选错而是**注册点**选错。`\meta` 的 stream capture 从内层 `\__codedoc_meta:n` 上移到公开 `\meta`（走通用 `cmd/meta/before|after` hook）并从专用适配器保留表移入通用注册表，因为 capture 入口现场求值 `\CJKecglue`，挂在 `\texttt` 内侧会让左边界取等宽字体的 5.25pt、右边界取正文字体的 3.33pt；否决手写重定义公开命令与让 ctxdoc 自行注册（后者被 #1010 的双表冲突检查正确拒绝，该拒绝本身即诊断信息）。hyperref 的行内锚点按两条出口 `\Hy@raisedlink` 与驱动层 `\hyper@anchor` 分别注册 `transparent`，否决 `post-transparent`（要求 marker 与末尾零尺寸盒子相邻，而 `\Hy@raisedlink` 在盒子前还排 `\penalty`）。记录已接受限制「无源码空格＋不可见命令＋紧接公式仍缺一枚 3.33pt」经未改动版本上的 `\textcolor{}`／`\null` 对照确认为既有。
- `llmdoc/memory/decisions/1038-tabular-cr-group-peek.md` — 决策：#1038 让 CJK→Boundary 的花括号分支只吸收触发转换的那一枚左花括号（`\afterassignment` + `\let`，不开分组），再前瞻其后首记号判断是否 `$`，最后补发隐式左花括号恢复分组；根因是 XeTeX 判断字符类时会展开宏且 `\protected` 无效，`tabular` 里 `\\`（`\@tabularcr`）展开后首记号是显式 `{`，原实现用 `n` 型参数吞掉 `{\ifnum0=`}` 再用隐式分组记号重发，破坏 LaTeX 平衡花括号技巧对显式 `}` 的要求；否决删除该分支（`command-boundary-math01/02` 会失败）与「重发显式花括号字符」（仍改变 `\@ifstar` 前瞻时序）；记录触发面只含 `tabular` 系：判据是替换文本首记号是否为显式 `{`（`\@arraycr` 用的是同一个平衡技巧但以 `$` 开头），而非「是否用了该技巧」；另记附带改善——隐式花括号 `\bgroup` 写法也拿回了 `\CJKecglue`。
- `llmdoc/memory/decisions/1037-ulem-word-front-ecglue.md` — 决策：#1037 修掉 #1026 同一机制的另一半——西文词**前**的 `\CJKecglue` 因源码空格被前视吃掉、`CJK-space` marker 落在 `\UL@start` 刚打开的片段盒内而固化收缩量；新增入口 `\@@_use_ecglue_skip:`，`xeCJK` 主体默认为普通 `\skip_horizontal:N`、由 `xeCJKfntef` 改写为经 `\@@_ulem_glue:n` 输出（`\UL@stop` 关盒、间距画成外层 leaders、`\UL@start` 开新盒）；否决 `\@@_boundary_use_ulem_glue:n`（放裸 glue，词前留 7px 无装饰线断口）与在主体直接引用子包函数（不加载 xeCJKfntef 即报未定义）；盲审以 blocking finding 指出改写不能只依赖 `\xeCJK_if_ulem_patch:TF`，须先测装饰 stream 布尔，否则 `nath`／`morehype` 等重定义 `\ ` 的文档中不含装饰命令的中西文混排即报错；测试判据由「溢出恰为 3.64pt」改为「无 `Overfull`」并新增 `\badness` 正向断言，记录重排路径那 1.11pt 仍属已接受限制。
- `llmdoc/memory/decisions/382-dash-width-and-ligature-opt-in.md` — 决策: #382 破折号宽度修复分两阶段——公式修正（`\@@_long_punct_kerning:N` 三路取大 kern + `\xeCJK_punct_margin_process:NN` 全份 margin 补偿）默认生效, OpenType 合字支持通过 `PoZheHao` 字符类 `PoZheHaoLigature` opt-in; margin 选择新增专用条件 `\@@_punct_if_full_margin_dash:N` 而非改变目标宽度基准; 合字选择用户显式开关而非自动探测字体特性。
- `llmdoc/memory/decisions/431-latinpunct-option.md` — 决策: #389/#431 新增 `LatinPunct` 选项让中西文共用码位标点（弯引号/间隔号/省略号）可切换为西文字体输出；归入 `Half*` 类而非 `Default`；破折号/半字线刻意排除保持与 `PoZheHaoLigature` 正交；状态记录改用局部布尔 `\l_@@_latin_punct_bool` 并回溯修正 `PoZheHaoLigature` 同类作用域问题（影子布尔作用域必须与被控 `\XeTeXcharclass` 资源一致）。
- `llmdoc/memory/decisions/336-external-interchar-class-others.md` — 决策: #336 的 CJK URL 断行由既有 `Others` 兼容层覆盖；外部 class 必须在导言区结束前定义 Default transition，不新增通用字符类继承 API。
- `llmdoc/memory/decisions/347-boxed-glyph-transform-prototype.md` — 决策: #347 的 interchar 装盒变换保留为未来整体重构的设计原型，但逐 code point 装盒无法局部接入当前 class/shaping/Boundary 状态机，现阶段 `not planned`。
- `llmdoc/memory/decisions/510-ruby-compatibility-boundary.md` — 决策: #510 的旧 `ruby.sty` crash 已由禁载 `CJK.sty` 解决，但这不构成传统 CJK 私有协议兼容；推荐 PXrubrica，不模拟旧 kern-marker 状态机。
- `llmdoc/memory/decisions/553-mixed-font-spacing-class-not-planned.md` — 决策: #553 的“CJK 字体 + Default 间距”在 XeTeX 上技术可行，但混合类会破坏 xeCJK 的 CJK/非 CJK 二分；当前采用显式局部字体命令，不增加数字特例并以 `not planned` 关闭。
- `llmdoc/memory/decisions/808-inline-code-verb-addon.md` — 决策: #808 的真实需求是行内代码网格而非字体族级 `CJKecglue`；组合等宽字体与 `\xeCJKVerbAddon`，不改变全部 `\texttt` 的断行行为。
- `llmdoc/memory/decisions/859-gh-assets-orphan-branch.md` — 决策: 建立长期 orphan 分支 `gh-assets` 集中托管 issue/PR 讨论静态资源, 取代按事件建临时分支（`tmp-859-assets` / `tmp-456-assets`, 均已删除）; 添加新资产须用 worktree 或纯 plumbing 流, 禁止主工作区 `git checkout --orphan` 以避免 `git clean` 波及未跟踪文件。
- `llmdoc/memory/decisions/456-longpunct-kinsoku-both-sides.md` — 决策: #456 长标点与其他标点相邻断点改为"两侧禁则联合判断"，落在既有 `\@@_punct_kern:NN` 内新增辅助函数 `\@@_punct_kern_break:NN`，不新增字符类/special punct 属性、不引入 penalty 类机制；`\g_@@_last_punct_tl` 参与 `\@@_punct_if_right:N` 前需 `\exp_after:wN` 展开为字符记号，参与 `\@@_punct_if_long:N` 判断则直接传 tl。
- `llmdoc/memory/decisions/908-ubuntu-fontset-fangsong.md` — 决策: #908 `fontset=ubuntu` 补齐仿宋 `zhfs`/`\fangsong`，采用朱雀仿宋（lxgw-fonts）→ FandolFang → Noto 宋体三级运行时 fallback（仅 XeTeX/LuaTeX，`\fontspec_font_if_exist:nTF`）；基线定量（dp: Noto 0.76pt / 朱雀 1.21pt / Fandol 1.75pt）驱动候选排序；`\CJKsymbol`+`\raisebox` 基线抬升 hack 验证有效但判定与标点压缩系统冲突，ctex 不提供该功能；`DEPENDS.txt` 新增 `soft lxgw-fonts`。
- `llmdoc/memory/doc-gaps.md` — 已知文档与实现缺口追踪。
- `llmdoc/memory/archive/2026-07-20/999-command-boundary-capture-framework.md` — 反思: #999 先用完整矩阵把 edge case 收敛为有限节点形状，再建立 capture/register；记录运行时首尾类别、嵌套与 `\sbox`、宽度/节点/可视三层证据、说明层隔离、lazy font 预热、同构 glue 支持边界，以及 PR 预览与 #992 活表的合并状态分层。
- `llmdoc/memory/reflections/1057-fntef-nest-linebreak.md` — 反思：#1057（documentation 标签）报告 `xeCJKfntef` 装饰命令嵌套时断行异常，判定为 `ulem` 的固有约束而非回归缺陷（同一个 10cm 页宽的 MWE 在发布版 v3.10.4 与工作树 v3.10.5 下溢出量同为 276.99pt），处置为补文档加补测试、不改实现；最有价值的一条是**换一个可观察量能给原本「无法回归保护」的路径补上测试**——#1026 记载 `\UL@onin` 因 `\setbox\UL@box\hbox{{#1}}` 使收缩量出不来而不可测，改用「能否断行」这个受盒子刚性影响的量就落在了同一条路径上（并用计数器插桩确认分派：线型套线型计数 1、线型套符号型计数 0）。另记「把既有限制固定成断言需要双向判据」（含与不含 Overfull 两侧各有断言并各做变异验证）、「文档里的肯定性清单必须声明它保证的是哪一个维度」（那份「间距行为正确」的清单没说自己只管间距，报告者据此外推成「嵌套没问题」）、「报告者的复合样例不能用来划定支持边界」（原 4 个案例同时换命令、顺序与正文长度，只跑它们会把 `\CJKunderdot` 的嵌套误说成不能断行）、以及实际加载路径核对的又一次发作与「样例不能同时触发两条既有限制」（字面正文与宏承载正文在同一个探索 MWE 上给出同一个 276.99pt，不复核就分不清量到了哪一条）。
- `llmdoc/memory/reflections/1067-ulem-brace-group-ecglue-shrink.md` — 反思：#1067 修复花括号分组使 `ulem` 装饰正文内 `\CJKecglue` 收缩量固化的问题（`\CJKunderline{虚室 {hello} 生白}`，花括号在片段盒内部才展开成分组，`\@@_ulem_glue:n` 的 group tag 守卫据此拒绝搬运）；修法是新增 `\@@_ulem_defer_glue:n`／`\@@_ulem_flush_pending_shrink:`，把间距拆成盒内不可伸缩 kern 占自然宽度加全局记账、到词尾（已在盒外分组外）补一枚零宽带伸缩 glue 两半输出，守卫本身未改动。核心教训是同一枚「绕过守卫后 badness 读数不变」的证据被两次误读成方向相反的结论——先被遗留记载为「守卫拒绝搬运，刻意设计」（止步于表面），后在本次探索阶段被反向误读成「与守卫无关，根因是编组改变了切词结果」（跳步推理：从「改掉它无效」直接推出「它不是原因」，而正确答案是两者都成立且互不矛盾）；这是 [[1046-1047-meta-anchor-font-context]] 确立的「现象、联系、穷尽性、成因是四个独立命题」在成因层的镜面发作。第二次误读还导致过早判定「不可修复、只能文档化」并开出一个后来被关闭的纯文档 PR（#1070）；根因更正后必须重新评估全部旧「不可行」结论，而不能只改被指出错误的那一句。另记贴图前须自己核对图上能否看出文字所称现象（关闭 PR 里贴的对比图实际溢出仅 0.676pt，170dpi 下不到 2 像素）、同一函数在主体与子包各有一份定义时改错位置会得到假的「变异全绿」、以及连续两轮共 9 个本地盲审代理（含更早 #366 的 4 个）分析完成但未能交出报告，建议后续盲审改为让审查员把报告直接写到约定路径的文件。
- `llmdoc/memory/reflections/1068-selectfont-resets-ccglue.md` — 反思：#1068 修复 `\selectfont` 经 `\ctex_update_stretch:` 的 `auxiii` 分支重置用户已设汉字间距（LuaTeX 下 `kanjiskip`、upTeX 下同名原语）；根因是 `ctex-engine.dtx` 里给 `\@@_update_stretch_auxii:` 补 `\ctex_if_ccglue_touched:` 守卫的那段 `\ctex_at_end:n` 被 docstrip 守卫限定在 `%<*pdftex|xetex>`，luatex/uptex 从未执行过它；修法是删掉那两行 docstrip 守卫标记，让重定义对全部引擎生效，三套引擎各自的判断逻辑早已存在、未新增代码。两条核心教训：**缺陷按代码路径分布，不按报告者用的引擎分布**——报告只提 LuaTeX，实测 upTeX 同样受影响，pdftex/xetex 正常；**守卫「存在」不等于「被调用」**——第一版分析把「`auxiii` 缺少守卫」误诊为「需要新写」，据此设计了两套在 kernel 层新增等价逻辑的方案，`grep` 全仓才发现守卫早已存在于 engine 层、只是被守卫条件挡在两个引擎之外，真实修法是删两行标记而非新增代码（发现「某处缺少检查」时先搜索该检查是否已在别处存在，是「排查前先查已有记录」这条规则从「查文档」到「查代码实现」的推广）。另记既有 `ccglue01`／`ccglue02.lvt` 对 LuaTeX／upTeX 打 `not tested yet.` early-exit、恰好跳过了唯一会坏的两个引擎（`not tested yet` 与 CI 的 `::notice::...跳过` 同属静默放行）；`\ccwd = kanjiskip + \zw` 连带变化用既有的 `linestretch=\maxdimen` 路径对照后确认不是本次副作用；以及 `linestretch` 无法作类选项（键空间不同，未知类选项被静默转发给标准文档类丢弃）这一既有限制，本次未处理、留作 Follow-up。
- `llmdoc/memory/reflections/1080-upstream-tocloft-fontspec.md` — 反思：#1080 排查 master 定时回归四红（`github472-03/04` 四引擎全红、`files01/02` 仅 XeTeX／LuaTeX 红）时发现两个互不相干的上游根因——`tocloft` v2.3i→v3.0a 主版本跳变新增净宽为零的 kern 对、`fontspec` 起不再显式加载 `xparse` 只影响 `\listfiles` 文件清单——核心教训是**失败集合的分布差异是成因数量的线索**：两组失败的引擎分布不同（四引擎 vs 两引擎）本身就该被当成「不止一个成因」的信号，而不是先找到一个成因就把另一组失败也挂在它名下（一度误查「`tocloft` 是否间接影响 fontspec 加载链」）。另记诊断链路本身有缺口——`_test-package.yml` 的 diff artifact 只传了串行路径 `<pkg>/build/**/*.diff`，`scripts/check-parallel.sh` 的并行工作区 `tmp/parallel-check/<engine>/<pkg>/` 从未被覆盖，并行失败时零诊断信息，已补齐路径并新增 `Show test diffs` 步骤把 diff 正文打进日志；刷基线前逐份核对 diff 内容形态（只含净宽为零的 kern／只含文件名行增删）才敢 save，是「刷 `.tlg` 基线前先按上游根因分类」判据的补充步骤；以及同一包目录下 `l3build check` 不能并发跑（与 #1026 的坑同源）。
- `llmdoc/memory/reflections/1048-1050-upstream-l3backend-pgf-baseline-drift.md` — 反思：#1050 的 CI 红全部落在与本 PR 改动无路径交集的 `test-ctex`／`test-xeCJK`，排查分成两条独立的上游问题（l3backend 会自愈、pgf 不会回退），过程中三次误判：把本机复现当成 CI 会复现的证据（实际 master CI 重跑是绿的，真正原因是改 `.github/tl_packages` 让 TL bypass cache key 分叉，master 命中的是旧快照）、两次把替代版本 l3backend 放错位置（该放 `localdir` 而非 `testdir`，且 `TEXINPUTS` 会被 `l3build-check.lua:850` 覆盖）导致两次假阴性、以及 CLEVEREF 归因先跳过本仓库已有的 `cleveref-patch.md` 根因链直接去读上游源码。确立「本机能复现推不出 CI 会复现」「CI 缓存分叉时不能拿一侧的绿当基准」「注入类实验必须有可核实的生效判据」「排查上游问题前先查本仓库已有文档」等规则。**后续进展见文末追加的一节与 [[1054-l3backend-defense-scope-and-kpse-lsr]]**：#1054 把 workaround 抽成 `scripts/sync-l3backend.sh` 并接入 doc 与 release 两条此前无防御的路径；同一根因在 doc 路径不触发任何退出码，因此本反思讨论的判别手段（`.tlg` diff、缓存 key 对比）在那条路径不适用。
- `llmdoc/memory/reflections/1054-l3backend-defense-scope-and-kpse-lsr.md` — 反思：#1054 把 l3backend skew 防御抽成共享脚本并补上 doc／release 两条路径，方案本身一次就对，但落地出了四类问题，**其中三类都与「验证方式」而非「方案」有关**：把用户已写好的 makeindex 转义 `\&!=6` 当成笔误改掉，引入 `Extra }` 构建失败（`!` 是 `gglo.ist` 的 quote 字符）；第一次就猜对 ls-R 根因，却因在**不具备复现前提**的本地环境（`TEXMFHOME` 不在 `TEXMFDBS` 里、不带 `!!`，走磁盘搜索）做反证而撤回了正确修复，绕两条弯路（条件版 `mktexlsr`、用 `sed`／`perl` 删脚本片段做无效对照）后靠 CI 日志重新确认；`curl` 退出码 0 但只写出空文件，使失败延后到末尾并报出与真因无关的「注入未生效」；共享脚本漏纳入任何 CI 触发白名单，由两个 bot 独立指出（两种失效机制不同）。另记反直觉点——其他 doc job 靠 kpse「ls-R 比目录旧就回退扫盘」侥幸通过，`doc-zhmetrics` 因先跑过 `mktexlsr` 关掉回退而**偏偏是「刷过索引」的那个 job 失败**。
- `llmdoc/memory/reflections/1008-zhnum-counter-options-expansion.md` — 反思：#1008 的根因是带选项的 `\zhnum[opts]{counter}` 把计数器**名**写进 `.toc`（读回时计数器已归零），修法是先在 `\zhnum` 层把计数器展开为数值再交给处理数值的实现；排查中还发现 `\zhdigwithoptions` 多传一个参数的独立笔误，单独记 `\changes`、单独测。补测试时在「怎么观察不可展开命令的行为」上四次踩坑（`\typeout`／`\tl_set:Nx` 只拿到命令名，`\TEST` 参数内切 `\ExplSyntaxOn` 不生效因为参数已被读入 catcode 已冻结，`\protected@edef` 含 `@` 在 `\ExplSyntaxOn` 下报错），根源是「观察不可展开命令只有让它执行一条路」；盒子度量的判别力踩过两次：缺字时宽度 collapse 成同一值、改用高度才区分开「七／柒」，但高度同样不足以固定字形（「柒」「九」「佰」的 `ht` 都是 7.75，一个排错字的缺陷零度量 diff），最终补 TEST 4 用 `\loggingoutput` 把六个入口的盒子内容本身写进基线。反思还如实记下三处我自己写错又被逐轮盲审证伪的表述：中止成因（`\scrollmode` → `-halt-on-error`，连错两次）、`Fatal error occurred` 不能当判据（xetex 不打印且被日志读取截断）、以及把中止机制回溯成「删掉守卫零 diff」的成因（实际成因是恒真断言，核对 `29f3649b` 的基线段落数即可否证）。按引擎需求分两个 `testfiledir`（`counter-options01` 三引擎记号层面、`legacy-entry01` 仅 xetex 真排汉字），这是本仓库第三次撞上「`l3build check` 无按文件指定引擎的机制」这条约束。另记「手册措辞会塑造用户对是否设计限制的判断」——报告者被「不可展开」一句引导到「这是设计限制」，未修文档前该用法不可发现。盒子度量选宽度还是高度也踩过一次，缺字时宽度 collapse 成同一值、改用高度才有判别力。按引擎需求分两个 `testfiledir`（`counter-options01` 三引擎记号层面、`legacy-entry01` 仅 xetex 实际排出汉字），这是本仓库第三次撞上「`l3build check` 无按文件指定引擎的机制」这条约束。另记「手册措辞会塑造用户对「是不是设计限制」的判断」——报告者被「不可展开」一句引导到「这是设计限制」，未修文档前该用法不可发现。
- `llmdoc/memory/reflections/1043-halign-alignment-tab-in-boundary-args.md` — 反思：#1043 的四个 blocking 级问题是同一失效模式的连续发作，全落在同一句话的归因上——先是坏探针给出废读数（`&`=0）写成假事实；更正时为正确读数（`&`=4）编了个假归因（`\c_code_cctab`，实际要 `\cctab_select:N` 才生效；真正原因是 LaTeX 环境默认设 4 而 `\ExplSyntaxOn` 不改 38）；再更正时又把「不满足时的后果」错放到调用期（实际字面模式类别在读取时冻结，风险只在加载期）。由此确立「事实、原因、后果是三个独立命题，各自都要单独验证」与「TeX 代码要先问这个量是读取时定的还是执行时定的」。原「坏探针让假事实进了四份文档」——把 `\char_value_catcode:n` 直接塞进 `\iow_term:x` 会读出废数据（`{` 读成 0、`a` 读成 `01`），据此断言的「`&` 是 catcode 0、直接写 `{ & }` 会静默失效」经盲审实测为假（expl3 régime 下 `&` 本就是 catcode 4，那种写法照样工作）；另记「反向验证的变异点须与被断言的机制一一对应」「判定函数是否存在要用 `\cs_if_exist:NTF` 而非 grep（`\tl_replace_all:NVn` 实为原生）」「清理输入用惰性 token 占位而非删除」「多症状客诉要逐一验证复现」「门禁的判别力边界要写进测试注释」，「没实测就把数字和名字写具体」（把「也报错」具体化成未经实测的 `Misplaced alignment tab`——实测它只在部分多列语境下出现、还取决于出错单元后是否仍有可用列；以及把两个不可比的错误计数并列成「50／52 → 0」），以及元教训：七轮独立审查共 29 个 finding、28 个由盲审发现（仅 1 个靠自己逐句自查），自检失效的模式是只验证了「改完好了」而没验证「理由是否为真」。
- `llmdoc/memory/reflections/1046-1047-meta-anchor-font-context.md` — 反思：#1046／#1047 的三条机制规则（注册点的字体上下文是语义的一部分、策略选择按实际节点次序而非有无可见输出、同一公开命令可能有多条实现出口且判据是读分派函数的分支）与本次最严重的判断错误——**既有测试全绿不构成「缺陷不存在」的证据**：自动分析把可 grep 的代码事实归因成视觉错觉，而它引用的 `codedoc-meta-ecglue01` 模拟内层函数时省掉了 `\texttt`，正是缺陷的必要条件（与 #1038 的 `tabular01` 零判别力同源：测试用简化替身时，简化掉的那一层可能正是缺陷所在）。另记「未发现代码路径」这类否定性结论须说明搜索了什么模式、为什么能穷尽；测量类用例被**被测命令**占用低位 box 寄存器（`\meta` 内部经 `\ensuremath`）导致读到 0.0pt 与污染值；字体预热要覆盖被测命令自己切换的字形；`l3build` 单遍编译使 `\ref` 排出 `??`；以及 `tlmgr` 报无更新不等于本地各包自洽（`l3kernel` 79868 对 `l3backend` 78544，后端接口 `:n`→`:nN` 使颜色参数被当文本排出，连带 11 项测试失败），判断失败归属的可靠方法是同一环境跑 master 并逐字节比对 diff 文件。
- `llmdoc/memory/reflections/1041-xecjk-version-gate.md` — 反思：#1041 的核心教训是「白名单式校验默认放行」——`::notice::` 不是 failure，xeCJK 从未接入却无人察觉。另记 `version` 全局名被 l3build 占用（须判类型）、`\ExplFileDate` 是日期占位宏而大括号里才是版本号（按 `\ProvidesExplPackage` 参数顺序读，别按宏名读）、「重新生成 + diff」校验的 diff 范围必须等于写入范围且要在干净 worktree 验证、以及新增校验应复现原事故验证判别力。
- `llmdoc/memory/reflections/1038-tabular-cr-group-peek.md` — 反思：#1038 的核心认知错误是把「`\protected` 宏不会被展开」当成通用事实（它只对完全展开语境有效，对 XeTeX 类别前瞻无效）；另记「包装被测函数会扰乱前瞻状态，反直觉结果先怀疑探针」「interchartoks 里抓参数会破坏别的宏正在执行的语法，应最小吸收」「既有 `tabular01` 因每行 `\\` 前有空格而零判别力——样例空白决定代码路径」「多个复现用例必须分文件跑，否则前一个报错导致后面假绿」。
- `llmdoc/memory/reflections/1037-ulem-word-front-ecglue.md` — 反思：#1035 合并后报告者追问 After 是否为预期，暴露 #1026 只修了同一机制的一半；记录「三个对照点缺了未受影响的发布版就会把『修回发布版』当成『修好』」「测试把残留缺陷冻结成预期基线，比没有测试更糟」「`\hbox to` 的实际宽度恒等于目标宽度、断言结构上恒真」「探针静默不等于路径不存在」「搬 glue 到外层可能弄断装饰线，须比像素」「通用路径引用可选子包函数会把它变成必需依赖」，以及两轮盲审 blocking finding 带来的「复用带守卫函数须重验守卫在新调用点的前置条件」「改通用路径时『加载子包但不使用该功能』是独立验证象限」「状态布尔为真不等于资源可用，置真点与复位点要成对清点」「根因是代码事实，须写成可 grep 的模式并穷举全部出现位置」「穷举的可信度取决于探针粒度与结论粒度一致」「分支级改动需要分支级断言」「写『判别力已实测』前必须真的跑那次变异」等教训。
- `llmdoc/memory/reflections/1005-xcjkecglue-right-boundary-recovery.md` — 反思：PR #1005 修复 #1003 时，类别 marker 正确但右边界仍缺少外层 `spacefactor` 和 `marker + glue` 的物理相邻关系；记录有界 post-transparent 节点移动、0pt glue 节点契约、1664 个矩阵断言、节点级验证、changelog 校验、并行 LuaTeX 临时日志缺失，以及合并后从 16 个驱动复验并更新 #992 活表的收尾过程。
- `llmdoc/memory/reflections/1002-inline-math-boundary.md` — 反思：PR #1009 将 #1002 纳入 capture/register 框架时，源码公式语法只能产生候选，须由实际节点确认；记录 `math-space`／`math-space-frozen`、弹性间距与实际断行、不可见节点使 marker 过期、左右边界独立验证、PR 图示，以及从独立子代理审查收紧到可审计上下文隔离校验的过程。
- `llmdoc/memory/reflections/1010-boundary-register-public-api.md` — 反思：#1010 开放实验性注册入口时，真正困难的是策略语义和生命周期，而不是 l3keys；记录专用适配器保留表、保存／应用双阶段冲突检查、公式参数限制、左右边界独立测试、公共诊断日志和本地上下文隔离审查闭环。
- `llmdoc/memory/reflections/994-macnew-songti-regular.md` — 反思：#994 把 `macnew` 正文宋体从 Light 调整为 Regular 时，同步具名字体、TTC index、zhmap 和 SPA 度量；记录 Linux 配置检查与 macOS 字体运行时证据的区别、XeTeX 现场重测、LuaTeX-ja 嵌套字形节点探针，以及 `\ExplSyntaxOn` 宏定义中嵌入 Lua 的空格规则。
- `llmdoc/memory/reflections/717-experiment-cjkecglue.md` — 反思: #717 用 `ctex / experiment` 子路径统一暴露实验性 `CJKecglue` 接口，并记录 xeCJK 参数桥接、xkanjiskip 缓存同步与四引擎基线策略。
- `llmdoc/memory/reflections/715-hyperref-driverfallback.md` — 反思: TYPE 展开陷阱、l3build 命令拦截测试技巧。
- `llmdoc/memory/reflections/671-cjkpunct-rglue-nobreak.md` — 反思: CJKpunct #671 修复中的节点级调试技术与 `\unhbox` 测试模式。
- `llmdoc/memory/reflections/735-zhlineskip-split-leading-leak.md` — 反思: zhlineskip #735 split 行距泄漏的根因（TeX 分组层级）、vbox 尺寸回归测试策略与 l3build 框架补建。
- `llmdoc/memory/reflections/465-fntef-font-state-and-underdot-space.md` — 反思: xeCJKfntef #465 中 ulem 下字体状态跨分组丢失 + `\CJKunderdot` 的 `\ignorespaces` 吞空格，及诊断误判的教训。
- `llmdoc/memory/reflections/581-xecjk-zero-width-format-chars.md` — 反思: xeCJK #581 中零宽格式字符应在输入层忽略，而不是进入 interchar 字符分类。
- `llmdoc/memory/reflections/556-verb-xkanjiskip-lltjcore.md` — 反思: ctex #556 中从 autoxspacing 误判修正为”禁用 ltj-latex 后漏掉 lltjcore 的 `\verb` 补丁”，以及基于 `\showbox` 的节点级定位方法。
- `llmdoc/memory/reflections/378-lstinline-hash-doubling.md` — 反思: xeCJK #378 中 `\lstinline` 宏参数 `#` 双写的根因（rescan 的 stringification 再次双写 cat6 `#`）、catcode 12 vs active 的易错点、`\regex_replace_all` catcode class 匹配技巧。
- `llmdoc/memory/reflections/879-lstinline-parameter-tokens-charcode.md` — 反思: xeCJK #879 中 `\lstinline` 下 catcode 6 token 字符码丢失的根因——#378 catcode-class regex 方案的“替换端硬编码 codepoint”局限在 `\catcode\`\&=6` 场景被暴露，改为 `\tl_map_inline:Nn` + `\token_if_parameter:NTF` + `\char_generate:nn { \int_value:w ``##1 } { 13 }` 逐 token 保留原字符码；promotion 时需显式记录适用边界。
- `llmdoc/memory/reflections/407-char-interchar-bypass.md` — 反思: xeCJK #407 中 `\char` 原语被 interchar 拦截的根因、`\char` vs mathcode 语义差异、测试场景设计偏差。
- `llmdoc/memory/reflections/800-char-let-xint-compat.md` — 反思: xeCJK #800 中 `\char` 重定义必须延迟到 `\AtBeginDocument`，避免破坏 xint 等包在加载期 `\let` 保存原语的假设。
- `llmdoc/memory/reflections/315-252-476-xecjk-ecglue-fixes.md` — 反思: xeCJK #252/#476 的 ecglue 字体度量问题与 #315 一样属于 interchar 边界恢复链，应在正确 CJK 字体上下文中缓存前侧 ecglue，并提前按 CI 依赖链完整验证基线影响。
- `llmdoc/memory/reflections/756-802-spa-unicode17-baseline.md` — 反思: #756 XeTeX 字体查找语法陷阱、#802 Unicode 区块同步流程、xeCJK→ctex 跨包基线联动模式。
- `llmdoc/memory/reflections/807-set-color-stale-state.md` — 反思: xeCJK #807 中 `\set@color` 无节点分支未清空 `\g_@@_last_node_tl`，导致首次 `\textcolor` 把初始化阶段残留的 `default` 误送入 whatsit 恢复链并错误插入 ecglue。
- `llmdoc/memory/reflections/809-810-hyperref-annot-ecglue.md` — 反思: xeCJK #809/#810 中 hyperref 注释起始 whatsit 需在 `\Hy@BeginAnnot` 保存/清空/选择性重放状态；#972 后明确该“开始端”结论只适用于入口污染，不覆盖结束 whatsit 遮蔽真实末节点的独立路径。
- `llmdoc/memory/reflections/324-boundary-reserve-space-glue.md` — 反思: xeCJK #324 中宏路径提前输出空格 glue 遮蔽 `CJK-space` 标记 kern，破坏 `\lastkern` 边界恢复；修复同时揭示 xeCJK→ctex 的广泛基线联动。
- `llmdoc/memory/reflections/826-fntef-boolean-flag-iteration.md` — 反思: xeCJK #826 fntef glue-on-kern-pair 初始修复后的迭代——`\l_@@_last_skip` 状态污染、`\quad` 误处理、fallback 路径错误的根因与三层过滤收敛过程。
- `llmdoc/memory/reflections/831-reset-color-pending-bool.md` — 反思: #831 colorbox/textcolor 右侧间距修复四轮迭代——从 `\reset@color` 直接插入 kern 对到 `\g_@@_reset_color_pending_bool` 专用布尔延迟处理的收敛过程，核心教训为共享全局布尔的语义过载风险。
- `llmdoc/memory/reflections/873-880-meta-url-hbox-math-boundary.md` — 反思: xeCJK #873 / #880 中 `\HD@target` 的 0x0 hbox 与 `\Url@FormatString` 的 math 模式分别遮蔽边界 marker；混合修复（save/replay vs drain），以及本地 TeX Live usertree 与 CI 漂移导致 7 个 false-positive 测试失败的诊断教训（双步同步 + fmt 重生成）。
- `llmdoc/memory/reflections/910-verb-null-hbox-drain.md` — 反思: xeCJK #910 的旧 verb drain 不能在无 marker 时 clear `\g_@@_last_node_tl`；该 helper 后更名为 keep-state，`\verb` 在 #992 已迁移为 auto stream，但这份记录仍解释 FandolFang→FandolSong 的 CJK→CJK 状态要求。
- `llmdoc/memory/reflections/931-biblatex-pagetracker-let-shadow.md` — 反思: xeCJK #931 中 biblatex `authoryear` 通过 `\let\blx@pagetracker\blx@pagetracker@context` 在 bbx 加载时把 pagetracker 行为**值拷贝**——首次 patch 挂在 `\let` 源函数（`\blx@pagetracker@context`）不生效，`\iow_term:x` 打点无输出的假信号误导；正确修法是补丁点改到 `\let` 目标本身 + hook 时机延迟到 `\@@_at_end_preamble:n`（`\@@_package_hook:nn` 对 nested style-load 太早）。补丁点选择的第三维度：目标控制序列的绑定形态（`\def` vs `\let` 拷贝目标）。
- `llmdoc/memory/reflections/935-check-doc-zhspacing-blockers.md` — 反思: #935 新增 `check-doc.yml` PR 校验跑 `l3build doc` 时首轮 CI 暴露 3 包 typeset 缺陷 (xpinyin fontconfig 索引缺失 / zhmetrics TL 包不含顶层 tfm/map / zhspacing 深层依赖). 关键教训: `l3build ctan` 内部硬编码跑 check 不能作 PR 校验替代品; fontconfig alias 三种姿势都救不了 XeTeX/fontspec 的字体查找 (`fc-match` 生效但 `\newfontfamily` 依然找不到), CI 上给不存在的字体提供替代唯一稳定办法是 sed patch dtx/sty; nonstopmode 会掩盖 fatal 之后错误链条 (zhspacing 首层 SimSun 修好后暴露 `\@iforloop` undefined 假象); TL 里的包不必等价于 CTAN 项目产物 (zhmetrics 顶层 tfm/map 是 CTAN admin 手工上传的独立文件, TL 打包未纳入); zhspacing 从 caller 删除留 followup 而非强行修完.
- `llmdoc/memory/reflections/937-ctex-split-version-stamp-ci.md` — 反思: PR #937 ctex.dtx 拆成 6 文件 (kernel/auxpkg/engine/scheme/fontset + 主 dtx 保留 ins/README/手册) 的区域规划与配套版本 CI. 关键教训: "回写 git 元数据到源文件"的机制必须有收敛条件否则永动 (stamp commit 产生新 sha → 下次又要回写), ctex update_tag 用"版本号相等即跳过"守卫; awk 取 `$Id:` stamp 版本字段是 $4 不是 $3, 新校验脚本必须先本地模拟正反场景; `make tag` (git tag) 与 `l3build tag` (源文件 stamp) 是两回事; 共享 feature 分支 push 前 pull --rebase.
- `llmdoc/memory/reflections/878-xunicode-symbols-multilevel-fallback.md` — 反思: xeCJK #878 `xunicode-symbols.tex` 驱动从“整段单字体 if-else”升级为 `FreeSerif → Noto Sans Symbols 2 → Symbola → Segoe UI Symbol → DejaVu Sans` 五级逐字符 `\iffontchar` + `\cs_if_exist_use:N` 链；只适用于演示性符号目录驱动文件，不应推广到正文 / CJK 字体路径。
- `llmdoc/memory/reflections/456-longpunct-kinsoku-both-sides.md` — 反思: xeCJK #456 长标点断点两侧禁则修复中"标点属性判断"函数族参数形态不同（`\@@_punct_if_right:N` 吃字符记号需 `\exp_after:wN` 展开 tl，`\@@_punct_if_long:N` 直接吃 tl）、`punct.tlg` 大文件基线联动 diff 用"变化位置共同特征 + 节点变化统一模式"两条证据判定预期变化、以及标点对矩阵 + `\showbox` 节点判定的系统性禁则调试法。
- `llmdoc/memory/reflections/874-876-agentic-fork-shielding-cron.md` — 反思: #875 / #874 `agentic-*.yml` 同时存在两条边界约束——job 级 `if: github.repository == ...` 把 fork 调度挡在 runner 分配之前，`schedule` 频率回退到每天一次北京时间 08:00；未来新增 agentic 工作流时这两条都应作为默认。
- `llmdoc/memory/reflections/agentic-template-reuse.md` — 历史反思：复用 `agentic-workflow-template` 的 Issue 分派与 llmdoc 更新，用事件驱动分派取代定时巡检，并记录当时的薄调用层、固定提交引用、直接写入权限和离线合同测试边界；该运行方式已被 #1025 替代。
- `llmdoc/memory/reflections/1025-agentic-local-runtime-toolchain.md` — 反思：将三条远端 reusable Agent workflow 展开到本仓库，为六个 Agent job 安装排版与命令沙箱工具链；记录从“缓存绝对只读”修正为 Agent 启动前可信显式保存，并以字体完整性检查、专用用户、模型代理、Codex 空指令根与 Claude `--bare`、Agent 不可写的 base 固定规范、沙箱外结果控制目录、不可转储 CLI、固定事件提交、独立 publisher、按 head 的评论幂等性、完整分页和 `updated_at` 回复边界收紧运行合同。**其中三层进程隔离已被 #1032 取代**，见决策 [[1032-agent-runtime-simplification]]。
- `llmdoc/memory/reflections/1030-1031-composite-action-semantics.md` — 反思：PR #1030/#1031 修复 `setup-agent-tools` 复合 Action 的两处独立缺陷——`timeout-minutes` 只在 job step 合法、`run` 默认 shell 带 `pipefail` 使提前 `exit` 的 awk 管道以 141 终止 step；记录复合 Action 与 job step 两套字段/默认值语义、自建校验器允许表须核对平台实际拒绝行为、加载期失败遮蔽运行期缺陷，以及诊断误判 close/reopen 不能替代 rebase 恢复 `pull_request_target` 的 `base.sha`（分叉点）语义。**这两处缺陷是 #1032 否决三层隔离方案、改用单脚本工具安装的直接原因**，见决策 [[1032-agent-runtime-simplification]]；字段/默认值语义规则本身仍适用于 `run-agent`/`feishu-notify` 两个仍在用的复合 Action。
- `llmdoc/memory/reflections/2026-08-13-agentic-codex-fallback-status.md` — 反思：三条 Agent workflow 以 `continue-on-error` + `outputs.status` 保持 Codex 主链路失败可追踪并触发 Claude fallback；下游不得再判失去判别力的 `.result`，llmdoc 候选生成与校验需分别汇总；同时记录 Codex endpoint、priority 通道和 high 推理强度默认值。
- `llmdoc/memory/reflections/ctex-architecture-doc.md` — 反思: ctex 架构独立文档的创建过程、源码阅读方法与已知文档缺口。
- `llmdoc/memory/reflections/961-changelog-freshness-gate.md` — 反思: #961 CHANGELOG.md 生成物新鲜度校验（check-changelog.yml）流程分歧收敛过程、跨平台字节一致性必须由脚本自控 encoding/newline 的新坑、用改造前脚本输出当字节级 oracle 验证回归的方法、校验 fail 时按校验对象大小设计可操作性（整文件需三通道贴期望内容）、以及「生成物新鲜度校验」作为跨 #937/#961 的通用架构模式的提炼建议。
- `llmdoc/memory/reflections/1001-boundary-capture-gap-fixes.md` — 反思：PR #1001 修复 #996、#998、#1000 时，管道掩盖了六项测试失败；盒子是否直接排出可见内容需要同时检查尺寸和末节点类型；嵌套盒子结束时，外层盒子必须读取节点列表末尾的 marker，不能无条件覆盖所有外层 `last_tl`。文档还记录了两项错误旧基线和 gh-assets 测试驱动引用已删除内部变量的问题。
- `llmdoc/memory/reflections/1012-fntef-decoration-overlap.md` — 反思：#1012 先后否决清零 `\UL@pixel`、`1em/3` 加原 leaders 选择、朴素普通 `\leaders` 和 `\cleaders` 加胶水专用图形等不完整方案，保留普通 `\leaders` 的共享相位能力，再以首末裁切和断点两侧半周期连接补齐端点与断行接点；记录历史大字号重黑体验证的盲点、斜线纵向覆盖、嵌套状态必须按调用层拥有、实际加载路径核对，以及真实绘图度量、轻量节点替身、PDF 坐标校验、精确视觉 MWE 和整本文档构建的分工。
- `llmdoc/memory/reflections/1017-fntef-actualtext.md` — 反思：#1017 从字符型装饰污染 PDF 文本提取，收敛到空 `ActualText` 加最小范围 tagging 暂停；记录普通／tagged PDF 文本与 300 dpi 视觉的独立证据、CI 依赖漏项，以及 workflow 固定 SHA 的合同同步和隔离增量审查。
- `llmdoc/memory/reflections/1026-ulem-literal-body-outer-shrink.md` — 反思：#1026 修复 ulem 装饰右边界断行不对齐时，子系统标签指向 #1012 但真正引入点是更早的 #1002 提交 `494d5a72`；记录三次回归测试写错却都“通过”的假绿——单 `\hbox`/`\vbox` 的 glue set 会抹平内外层收缩区分，宏承载正文又触发另一条已接受的既有限制——以及必须用重新引入缺陷的方式确认测试会失败、`\showthe` 在 `\TEST` 中会中断执行并把残留节点列表固化进基线、并行 `save`/`check` 争用共享 `build/test` 目录、对比实验中再次核对实际加载路径的教训。
- `llmdoc/memory/reflections/1029-sbox-global-prefix.md` — 反思：#1029 中 `\global\sbox` 在 `cmd/sbox/before` 钩子内的赋值消耗前缀后静默丢失盒子内容，报告者已定位到两个钩子并给出删钩子变通；记录报告者定位止步于变通容易被直接采纳为修复、必须先核实两个钩子是 #992 系列刻意引入的 scratch box 隔离、缩小复现到不含 xeCJK 的五行纯 LaTeX 才确认是 `\AddToHook` 机制的通用陷阱而非本包特有，以及失败完全静默时唯一可靠证据是紧接赋值后与实际使用点的数值对比。
