\input{regression-test}

%% pdfTeX / CJKutf8 路线下 query 选项必须报错 (#550).
%%
%% 决策 9 与手册第 4 节都写明查询命令只在 XeTeX 下提供: 取汉字码位靠 `#1,
%% 在 CJKutf8 路线上一个汉字由多个字节组成, 取不到所需的码位。
%%
%% 单独成文件的两个原因:
%%   * 报错会中断编译 (checkopts 带 -halt-on-error), 报错之后不能再放别的项;
%%   * 这一行为在 pdftex 上才成立, 属 config-cjk 那条线。
%%
%% 判据是报错本身。若引擎门禁被删掉或改成放行 pdfTeX, 这里就不再报错, 基线随之
%% 变化 (实测把 \msg_error:nn { xpinyin } { query-needs-unicode } 删成空之后,
%% 两条线原本都是全绿 —— 这个缺口正是本用例要补的)。

%% \START 必须在加载 xpinyin 之前: 报错发生在宏包加载阶段, 若 \START 在
%% \begin{document} 之后, 编译在到达它之前就中止, 基线会是空文件 (实测过)。
\documentclass{article}
\usepackage{CJKutf8}

\START

\TYPE{=== query option must fail on pdfTeX ===}
\usepackage[query]{xpinyin}

\begin{document}
\END
\end{document}
