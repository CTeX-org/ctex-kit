\input{regression-test}

\documentclass{article}

\usepackage{xcolor}
\usepackage{xeCJK}
\setCJKmainfont{FandolSong-Regular.otf}

\newlength{\auditwidth}

\begin{document}

\START

\OMIT
% #995: measuring \colorbox content inside \settowidth must not leak
% capture/marker state into boxes built afterwards. Box pairs are built
% from identical source; the measurement runs between the two builds.
\setbox0=\hbox{丙\special{audit} 丁}
\settowidth{\auditwidth}{甲\colorbox{yellow}{乙}}
\setbox2=\hbox{丙\special{audit} 丁}
\setbox4=\hbox{丙 丁}
\settowidth{\auditwidth}{甲\colorbox{yellow}{乙}}
\setbox6=\hbox{丙 丁}
\TIMO

\TEST{settowidth colorbox keeps special box reproducible (995)}{
  \ifdim\wd0=\wd2
    \TYPE{PASS: identical special boxes agree}
  \else
    \TYPE{FAIL: \the\wd0 \space vs \the\wd2}
  \fi
}

\TEST{settowidth colorbox keeps plain box reproducible (995)}{
  \ifdim\wd4=\wd6
    \TYPE{PASS: identical plain boxes agree}
  \else
    \TYPE{FAIL: \the\wd4 \space vs \the\wd6}
  \fi
}

\END

\end{document}
