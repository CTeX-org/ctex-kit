% 本用例用 \listfiles 固定「加载了哪些文件」这份清单，因此它对**任何**上游宏包
% 增删依赖都敏感 —— 上游少 \RequirePackage 一个包，这里就会红，而那通常不是
% ctex 的回归。
%
% 已知发生过一次：#1080 里 fontspec 2.9h 起不再显式加载 xparse，四份基线各少一行
% xparse.sty。判断这类失败的办法是看 diff 是否只有文件名行的增删、且增删的那个文件
% 属于上游包；若是，刷基线即可，不必改 ctex。
%
% 反过来说，这份基线的稳定性依赖于上游的内部加载细节，本身是个脆弱点。它换来的是
% 能覆盖「ctex 自己该加载的文件有没有漏」，所以保留。

\input{regression-test}

\documentclass[fontset=fandol]{ctexart}

\listfiles

\begin{document}

\makeatletter
\ExplSyntaxOn

\cs_set_protected:Npn \@dofilelist
  { \clist_map_function:NN \@filelist \iow_log:n }

\ExplSyntaxOff
\makeatother

\START

\end{document}

\END
